<UserControl x:Class="Macro.Views.RecipeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Macro.Views"
             xmlns:viewModels="clr-namespace:Macro.ViewModels"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=viewModels:RecipeViewModel}"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </UserControl.Resources>
    
    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- 레시피 목록 -->
        <GroupBox Header="레시피 목록" Grid.Column="0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <ListBox ItemsSource="{Binding Recipes}" 
                         SelectedItem="{Binding SelectedRecipe}"
                         Margin="5"/>

                <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,5">
                    <Button Content="생성" Command="{Binding CreateCommand}" Width="80" Margin="5"/>
                    <Button Content="삭제" Command="{Binding DeleteCommand}" Width="80" Margin="5"/>
                </StackPanel>
            </Grid>
        </GroupBox>

        <!-- 상세 내용 -->
        <GroupBox Header="상세 정보" Grid.Column="1" Margin="10,0,0,0">
            <Grid>
                <TextBlock Text="레시피를 선택하거나 생성하세요." 
                           HorizontalAlignment="Center" 
                           VerticalAlignment="Center"
                           Visibility="{Binding SelectedRecipe, Converter={StaticResource BooleanToVisibilityConverter}, ConverterParameter=Inverse}"/>
                
                <StackPanel Visibility="{Binding SelectedRecipe, Converter={StaticResource BooleanToVisibilityConverter}}" Margin="10">
                    <TextBlock Text="{Binding SelectedRecipe.FileName, StringFormat='선택된 레시피: {0}'}" FontSize="16" FontWeight="Bold"/>
                    <TextBlock Text="{Binding SelectedRecipe.FilePath}" TextWrapping="Wrap" Margin="0,5,0,0" Foreground="Gray"/>
                </StackPanel>
            </Grid>
        </GroupBox>
    </Grid>
</UserControl>