<UserControl x:Class="Macro.Views.TeachingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:Macro.ViewModels"
             xmlns:models="clr-namespace:Macro.Models"
             xmlns:utils="clr-namespace:Macro.Utils"
             xmlns:components="clr-namespace:Macro.Views.Components"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1000"
             d:DataContext="{d:DesignInstance Type=vm:TeachingViewModel, IsDesignTimeCreatable=False}">

    <UserControl.Resources>
        <!-- Converter (Safety) -->
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <utils:UriToBitmapConverter x:Key="UriToBitmapConverter"/>
        <utils:EnumToBooleanConverter x:Key="EnumToBooleanConverter"/>

        <!-- Jump Target Template -->
        <DataTemplate x:Key="JumpTargetTemplate">
            <TextBlock Text="{Binding DisplayName}">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsGroup}" Value="True">
                                <Setter Property="FontWeight" Value="Bold"/>
                                <Setter Property="Foreground" Value="#EF6C00"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>
        </DataTemplate>

        <!-- ==================================================================== -->
        <!-- [Global DataTemplates] Direct Binding (Used by MultiAction Children) -->
        <!-- ==================================================================== -->
        
        <!-- 1. DelayCondition (Direct) -->
        <DataTemplate DataType="{x:Type models:DelayCondition}">
            <StackPanel Margin="5">
                <Grid Margin="0,0,0,5">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="대기 시간:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <TextBox Grid.Column="1" Text="{Binding DelayTimeMs, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Center"/>
                    <TextBlock Grid.Column="2" Text="ms" VerticalAlignment="Center" Margin="5,0,0,0"/>
                </Grid>
                <DockPanel Margin="0,5,0,0">
                    <TextBlock Text="실패 시 이동:" VerticalAlignment="Center" Width="80" Foreground="Gray"/>
                    <components:JumpTargetSelector ItemsSource="{Binding DataContext.JumpTargets, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                  SelectedId="{Binding FailJumpId, Mode=TwoWay}"/>
                </DockPanel>
            </StackPanel>
        </DataTemplate>

        <!-- 2. ImageMatchCondition (Direct) -->
        <DataTemplate DataType="{x:Type models:ImageMatchCondition}">
            <StackPanel Margin="5">
                <DockPanel LastChildFill="True" Margin="0,0,0,5">
                    <TextBlock Text="이미지:" VerticalAlignment="Center" Width="60"/>
                    <StackPanel Orientation="Horizontal" DockPanel.Dock="Right">
                        <Button Content="파일 선택" Width="70" Margin="5,0,0,0"
                                Command="{Binding DataContext.SelectImageCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                CommandParameter="{Binding .}"
                                ToolTip="Select existing image file"/>
                        <Button Content="화면 캡처" Width="70" Margin="5,0,0,0"
                                Command="{Binding DataContext.CaptureImageCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                CommandParameter="{Binding .}"
                                ToolTip="Snip from screen"/>
                    </StackPanel>
                    <TextBox Text="{Binding ImagePath, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Center" IsReadOnly="True" ToolTip="{Binding ImagePath}"/>
                </DockPanel>

                <!-- Image Preview -->
                <Border BorderBrush="#DDD" BorderThickness="1" Margin="0,0,0,5" Height="100" Background="#F8F8F8">
                    <Image Source="{Binding ImagePath, Converter={StaticResource UriToBitmapConverter}}" Stretch="Uniform" ToolTip="Image Preview"/>
                </Border>
                
                <DockPanel LastChildFill="True" Margin="0,0,0,5">
                    <TextBlock Text="일치율(Threshold):" VerticalAlignment="Center" Width="100"/>
                    <TextBlock Text="{Binding Threshold, StringFormat=P0}" DockPanel.Dock="Right" Width="40" TextAlignment="Right" VerticalAlignment="Center"/>
                    <Slider Minimum="0.1" Maximum="1.0" Value="{Binding Threshold}" TickFrequency="0.1" IsSnapToTickEnabled="True" VerticalAlignment="Center"/>
                </DockPanel>

                <UniformGrid Columns="2" Margin="0,0,0,5">
                    <DockPanel Margin="0,0,5,0">
                        <TextBlock Text="재시도 횟수:" VerticalAlignment="Center" Width="70"/>
                        <TextBox Text="{Binding MaxSearchCount, UpdateSourceTrigger=PropertyChanged}" ToolTip="이미지를 찾을 때까지 반복 시도할 최대 횟수입니다."/>
                    </DockPanel>
                    <DockPanel>
                        <TextBlock Text="재시도 간격:" VerticalAlignment="Center" Width="70"/>
                        <TextBox Text="{Binding SearchIntervalMs, UpdateSourceTrigger=PropertyChanged}" ToolTip="재시도 사이의 대기 시간(ms)입니다."/>
                        <TextBlock Text="ms" VerticalAlignment="Center" Margin="5,0,0,0"/>
                    </DockPanel>
                </UniformGrid>

                <!-- Test Section -->
                <Border Background="#E3F2FD" CornerRadius="4" Padding="8" Margin="0,0,0,10">
                    <StackPanel>
                        <Grid Margin="0,0,0,5">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,2">
                                    <TextBlock Text="Match Score: " FontWeight="Bold"/>
                                    <TextBlock Text="{Binding TestScore, StringFormat=P1}" Foreground="#1565C0" FontWeight="Bold"/>
                                </StackPanel>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="Result: " FontWeight="Bold"/>
                                    <TextBlock Text="{Binding TestResult}" Foreground="#0D47A1"/>
                                </StackPanel>
                            </StackPanel>
                            <Button Grid.Column="1" Content="🔍 Test Match" 
                                    Padding="10,5" Background="#2196F3" Foreground="White" FontWeight="Bold"
                                    Command="{Binding DataContext.TestImageConditionCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                    CommandParameter="{Binding .}"/>
                        </Grid>
                        
                        <!-- Test Result Image Preview -->
                        <Border BorderBrush="#90CAF9" BorderThickness="1" Background="White" Margin="0,5,0,0">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Setter Property="Visibility" Value="Visible"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding DataContext.TestResultImage, RelativeSource={RelativeSource AncestorType=UserControl}}" Value="{x:Null}">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <Image Source="{Binding DataContext.TestResultImage, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                   MaxHeight="400" Stretch="Uniform" ToolTip="Matching Result (Red: Found, Blue: ROI)"/>
                        </Border>
                    </StackPanel>
                </Border>

                <!-- ROI Settings -->
                <GroupBox Header="검색 영역 (ROI)" Margin="0,5,0,10">
                    <StackPanel>
                        <CheckBox Content="영역 지정 사용 (ROI)" IsChecked="{Binding UseRegion}" Margin="0,0,0,5"/>
                        <Grid IsEnabled="{Binding UseRegion}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            
                            <StackPanel Orientation="Vertical">
                                <UniformGrid Columns="2" Margin="0,0,0,5">
                                    <DockPanel Margin="0,0,5,0">
                                        <TextBlock Text="X:" VerticalAlignment="Center" Width="20" FontWeight="Bold"/>
                                        <TextBox Text="{Binding RegionX, UpdateSourceTrigger=PropertyChanged}"/>
                                    </DockPanel>
                                    <DockPanel>
                                        <TextBlock Text="Y:" VerticalAlignment="Center" Width="20" FontWeight="Bold"/>
                                        <TextBox Text="{Binding RegionY, UpdateSourceTrigger=PropertyChanged}"/>
                                    </DockPanel>
                                </UniformGrid>
                                <UniformGrid Columns="2">
                                    <DockPanel Margin="0,0,5,0">
                                        <TextBlock Text="W:" VerticalAlignment="Center" Width="20" FontWeight="Bold"/>
                                        <TextBox Text="{Binding RegionW, UpdateSourceTrigger=PropertyChanged}"/>
                                    </DockPanel>
                                    <DockPanel>
                                        <TextBlock Text="H:" VerticalAlignment="Center" Width="20" FontWeight="Bold"/>
                                        <TextBox Text="{Binding RegionH, UpdateSourceTrigger=PropertyChanged}"/>
                                    </DockPanel>
                                </UniformGrid>
                            </StackPanel>
                            
                            <Button Grid.Column="1" Content="영역 선택" 
                                    Margin="10,0,0,0" Padding="10,0"
                                    Command="{Binding DataContext.PickRegionCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                    CommandParameter="{Binding .}"/>
                        </Grid>
                    </StackPanel>
                </GroupBox>

                <DockPanel>
                    <TextBlock Text="실패 시 이동:" VerticalAlignment="Center" Width="80" Foreground="#C62828" FontWeight="Bold"/>
                    <components:JumpTargetSelector ItemsSource="{Binding DataContext.JumpTargets, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                  SelectedId="{Binding FailJumpId, Mode=TwoWay}"/>
                </DockPanel>
            </StackPanel>
        </DataTemplate>

        <!-- 3. GrayChangeCondition (Direct) -->
        <DataTemplate DataType="{x:Type models:GrayChangeCondition}">
            <StackPanel Margin="5">
                <Grid Margin="0,0,0,5">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <StackPanel Orientation="Vertical">
                        <UniformGrid Columns="2" Margin="0,0,0,5">
                            <DockPanel Margin="0,0,5,0">
                                <TextBlock Text="X:" VerticalAlignment="Center" Width="20" FontWeight="Bold"/>
                                <TextBox Text="{Binding X, UpdateSourceTrigger=PropertyChanged}"/>
                            </DockPanel>
                            <DockPanel>
                                <TextBlock Text="Y:" VerticalAlignment="Center" Width="20" FontWeight="Bold"/>
                                <TextBox Text="{Binding Y, UpdateSourceTrigger=PropertyChanged}"/>
                            </DockPanel>
                        </UniformGrid>
                        <UniformGrid Columns="2">
                            <DockPanel Margin="0,0,5,0">
                                <TextBlock Text="W:" VerticalAlignment="Center" Width="20" FontWeight="Bold"/>
                                <TextBox Text="{Binding Width, UpdateSourceTrigger=PropertyChanged}"/>
                            </DockPanel>
                            <DockPanel>
                                <TextBlock Text="H:" VerticalAlignment="Center" Width="20" FontWeight="Bold"/>
                                <TextBox Text="{Binding Height, UpdateSourceTrigger=PropertyChanged}"/>
                            </DockPanel>
                        </UniformGrid>
                    </StackPanel>

                    <Button Grid.Column="1" Content="영역 선택" 
                            Margin="10,0,0,0" Padding="10,0"
                            Command="{Binding DataContext.PickRegionCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                            CommandParameter="{Binding .}"/>
                </Grid>

                <UniformGrid Columns="2" Margin="0,0,0,10">
                    <DockPanel Margin="0,0,5,0">
                        <TextBlock Text="Threshold:" VerticalAlignment="Center" Width="70"/>
                        <TextBox Text="{Binding Threshold, UpdateSourceTrigger=PropertyChanged}"/>
                    </DockPanel>
                    <DockPanel>
                        <TextBlock Text="Delay:" VerticalAlignment="Center" Width="45"/>
                        <TextBox Text="{Binding DelayMs, UpdateSourceTrigger=PropertyChanged}"/>
                        <TextBlock Text="ms" VerticalAlignment="Center" Margin="3,0,0,0"/>
                    </DockPanel>
                </UniformGrid>
                <DockPanel>
                    <TextBlock Text="실패 시 이동:" VerticalAlignment="Center" Width="80" Foreground="#C62828" FontWeight="Bold"/>
                    <components:JumpTargetSelector ItemsSource="{Binding DataContext.JumpTargets, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                  SelectedId="{Binding FailJumpId, Mode=TwoWay}"/>
                </DockPanel>
            </StackPanel>
        </DataTemplate>

        <!-- 4. VariableCompareCondition (Direct) -->
        <DataTemplate DataType="{x:Type models:VariableCompareCondition}">
            <StackPanel Margin="5">
                <DockPanel LastChildFill="True" Margin="0,0,0,5">
                    <TextBlock Text="변수명:" VerticalAlignment="Center" Width="70"/>
                    <components:VariableSelector ItemsSource="{Binding DataContext.AvailableIntVariables, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                Text="{Binding VariableName, Mode=TwoWay}"/>
                </DockPanel>
                
                <Grid Margin="0,0,0,5">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="비교 연산:" VerticalAlignment="Center" Width="70"/>
                    <ComboBox Grid.Column="1" SelectedValue="{Binding Operator}" SelectedValuePath="Content">
                        <ComboBoxItem Content="=="/>
                        <ComboBoxItem Content="!="/>
                        <ComboBoxItem Content="&gt;"/>
                        <ComboBoxItem Content="&lt;"/>
                        <ComboBoxItem Content="&gt;="/>
                        <ComboBoxItem Content="&lt;="/>
                        <ComboBoxItem Content="Contains"/>
                    </ComboBox>
                </Grid>

                <DockPanel LastChildFill="True" Margin="0,0,0,10">
                    <TextBlock Text="비교값:" VerticalAlignment="Center" Width="70"/>
                    <TextBox Text="{Binding TargetValue, UpdateSourceTrigger=PropertyChanged}"/>
                </DockPanel>

                <DockPanel>
                    <TextBlock Text="조건 만족 시 이동 (True):" VerticalAlignment="Center" Width="140" Foreground="#2E7D32" FontWeight="Bold"
                               ToolTip="조건이 참(True)일 때 지정된 스텝으로 이동합니다. 거짓이면 다음 스텝으로 진행합니다."/>
                    <components:JumpTargetSelector ItemsSource="{Binding DataContext.JumpTargets, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                  SelectedId="{Binding FailJumpId, Mode=TwoWay}"/>
                </DockPanel>
            </StackPanel>
        </DataTemplate>

        <!-- 5. SwitchCaseCondition (Direct) -->
        <DataTemplate DataType="{x:Type models:SwitchCaseCondition}">
            <StackPanel Margin="5">
                <DockPanel LastChildFill="True" Margin="0,0,0,5">
                    <TextBlock Text="변수명:" VerticalAlignment="Center" Width="70"/>
                    <components:VariableSelector ItemsSource="{Binding DataContext.AvailableIntVariables, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                Text="{Binding TargetVariableName, Mode=TwoWay}"
                                                ToolTip="Target Variable Name (Integers only)"/>
                </DockPanel>

                <TextBlock Text="케이스 목록 (Cases):" FontWeight="Bold" Margin="0,5,0,5"/>
                
                <ItemsControl ItemsSource="{Binding Cases}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="0,0,0,5">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="60"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                
                                <TextBlock Text="값(Int):" VerticalAlignment="Center" Margin="0,0,5,0"/>
                                <TextBox Grid.Column="1" Text="{Binding CaseValue, UpdateSourceTrigger=PropertyChanged}"/>
                                
                                <TextBlock Grid.Column="2" Text="→ 이동:" VerticalAlignment="Center" Margin="10,0,5,0"/>
                                <components:JumpTargetSelector Grid.Column="3" 
                                                              ItemsSource="{Binding DataContext.JumpTargets, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                              SelectedId="{Binding JumpId, Mode=TwoWay}"/>

                                <Button Grid.Column="4" Content="-" Width="25" Margin="5,0,0,0" Background="#FFEBEE" Foreground="Red"
                                        Command="{Binding DataContext.RemoveSwitchCaseCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding .}"
                                        ToolTip="Remove Case"/>
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <Button Content="+ 케이스 추가" 
                        Command="{Binding DataContext.AddSwitchCaseCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                        CommandParameter="{Binding .}"
                        Margin="0,5,0,10" Background="#E8F5E9"/>

                <DockPanel>
                    <TextBlock Text="실패 시 이동:" VerticalAlignment="Center" Width="80" Foreground="#C62828" FontWeight="Bold"/>
                    <components:JumpTargetSelector ItemsSource="{Binding DataContext.JumpTargets, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                  SelectedId="{Binding FailJumpId, Mode=TwoWay}"/>
                </DockPanel>
            </StackPanel>
        </DataTemplate>

        <!-- 6. ProcessRunningCondition (Direct) -->
        <DataTemplate DataType="{x:Type models:ProcessRunningCondition}">
            <StackPanel Margin="5">
                <!-- Input Source Selection -->
                <TextBlock Text="입력 소스 (Input Source):" Margin="0,0,0,5" Foreground="Gray"/>
                <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                    <RadioButton Content="직접 입력 (Direct)" GroupName="ProcRunSrc" Margin="0,0,15,0"
                                 IsChecked="{Binding SourceType, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter=Direct}"/>
                    <RadioButton Content="변수값 (Variable)" GroupName="ProcRunSrc"
                                 IsChecked="{Binding SourceType, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter=Variable}"
                                 ToolTip="변수에 저장된 문자열을 대상 이름으로 사용합니다."/>
                </StackPanel>

                <DockPanel LastChildFill="True" Margin="0,0,0,10">
                    <TextBlock Text="검색 기준:" VerticalAlignment="Center" Width="70"/>
                    <ComboBox ItemsSource="{Binding DataContext.SearchMethods, RelativeSource={RelativeSource AncestorType=UserControl}}"
                              SelectedItem="{Binding SearchMethod}"/>
                </DockPanel>

                <!-- Direct Mode Input -->
                <DockPanel LastChildFill="True" Margin="0,0,0,10">
                    <DockPanel.Style>
                        <Style TargetType="DockPanel">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding SourceType}" Value="Direct">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </DockPanel.Style>
                    <TextBlock Text="대상 이름:" VerticalAlignment="Center" Width="70"/>
                    <Button DockPanel.Dock="Right" Content="⟳" Width="25" Margin="5,0,0,0"
                            Command="{Binding DataContext.RefreshContextTargetCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                            CommandParameter="{Binding .}"
                            ToolTip="Refresh Process List"/>
                    <components:VariableSelector ItemsSource="{Binding DataContext.ProcessList, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                Text="{Binding ProcessName, Mode=TwoWay}"
                                                ToolTip="Process Name or Window Title Part"/>
                </DockPanel>

                <!-- Variable Mode Input -->
                <DockPanel LastChildFill="True" Margin="0,0,0,10">
                    <DockPanel.Style>
                        <Style TargetType="DockPanel">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding SourceType}" Value="Variable">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </DockPanel.Style>
                    <TextBlock Text="변수명:" VerticalAlignment="Center" Width="70"/>
                    <components:VariableSelector ItemsSource="{Binding DataContext.AvailableIntVariables, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                Text="{Binding ProcessNameVariable, Mode=TwoWay}"
                                                ToolTip="대상 이름을 담고 있는 변수를 선택하세요."/>
                </DockPanel>

                <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                    <TextBlock Text="조건 확인:" VerticalAlignment="Center" Width="70" Margin="0,0,5,0"/>
                    <RadioButton Content="실행 중 (Running)" GroupName="ProcRun" 
                                 IsChecked="{Binding IsCheckRunning, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter=True}" 
                                 Margin="0,0,15,0"/>
                    <RadioButton Content="종료됨 (Not Running)" GroupName="ProcRun"
                                 IsChecked="{Binding IsCheckRunning, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter=False}"/>
                </StackPanel>

                <UniformGrid Columns="2" Margin="0,0,0,10">
                    <DockPanel Margin="0,0,5,0">
                        <TextBlock Text="재시도 횟수:" VerticalAlignment="Center" Width="70"/>
                        <TextBox Text="{Binding MaxSearchCount, UpdateSourceTrigger=PropertyChanged}" ToolTip="조건을 만족할 때까지 반복 시도할 최대 횟수입니다."/>
                    </DockPanel>
                    <DockPanel>
                        <TextBlock Text="재시도 간격:" VerticalAlignment="Center" Width="70"/>
                        <TextBox Text="{Binding SearchIntervalMs, UpdateSourceTrigger=PropertyChanged}" ToolTip="재시도 사이의 대기 시간(ms)입니다."/>
                        <TextBlock Text="ms" VerticalAlignment="Center" Margin="5,0,0,0"/>
                    </DockPanel>
                </UniformGrid>

                <DockPanel>
                    <TextBlock Text="실패 시 이동:" VerticalAlignment="Center" Width="80" Foreground="#C62828" FontWeight="Bold"/>
                    <components:JumpTargetSelector ItemsSource="{Binding DataContext.JumpTargets, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                  SelectedId="{Binding FailJumpId, Mode=TwoWay}"/>
                </DockPanel>
            </StackPanel>
        </DataTemplate>

        <!-- ==================================================================== -->
        <!-- [DataTemplates] Action Models (Direct) -->
        <!-- ==================================================================== -->

        <!-- 1. IdleAction (Direct) -->
        <DataTemplate DataType="{x:Type models:IdleAction}">
            <StackPanel Margin="5">
                <Grid Margin="0,0,0,5">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="대기 시간:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <TextBox Grid.Column="1" Text="{Binding DelayTimeMs, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Center"/>
                    <TextBlock Grid.Column="2" Text="ms" VerticalAlignment="Center" Margin="5,0,0,0"/>
                </Grid>
                <DockPanel Margin="0,5,0,0">
                    <TextBlock Text="실패 시 이동:" VerticalAlignment="Center" Width="80" Foreground="Gray"/>
                    <components:JumpTargetSelector ItemsSource="{Binding DataContext.JumpTargets, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                  SelectedId="{Binding FailJumpId, Mode=TwoWay}"/>
                </DockPanel>
            </StackPanel>
        </DataTemplate>

        <!-- 2. MouseClickAction (Direct) -->
        <DataTemplate DataType="{x:Type models:MouseClickAction}">
            <StackPanel Margin="5">
                <!-- Source Selection -->
                <TextBlock Text="좌표 소스 (Coordinate Source):" Margin="0,0,0,5" Foreground="Gray"/>
                <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                    <RadioButton Content="지정 좌표 (Fixed)" GroupName="CoordSource" Margin="0,0,15,0"
                                 IsChecked="{Binding SourceType, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter=Fixed}"/>
                    <RadioButton Content="찾은 좌표 (Found)" GroupName="CoordSource" Margin="0,0,15,0"
                                 IsChecked="{Binding SourceType, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter=Found}"
                                 ToolTip="Pre-Condition(이미지/Gray)에서 찾은 위치를 사용합니다."/>
                    <RadioButton Content="변수 좌표 (Var)" GroupName="CoordSource"
                                 IsChecked="{Binding SourceType, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter=Variable}"
                                 ToolTip="변수에 저장된 값을 좌표로 사용합니다."/>
                </StackPanel>

                <!-- 1. Fixed Mode Input -->
                <Grid Margin="0,0,0,5">
                    <Grid.Style>
                        <Style TargetType="Grid">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding SourceType}" Value="Fixed">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Grid.Style>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="X:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                    <TextBox Grid.Column="1" Text="{Binding X, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,10,0"/>
                    <TextBlock Grid.Column="2" Text="Y:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                    <TextBox Grid.Column="3" Text="{Binding Y, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,10,0"/>
                    <Button Grid.Column="4" Content="현재위치" Padding="5,0" 
                            Command="{Binding DataContext.PickCoordinateCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                            CommandParameter="{Binding .}"
                            ToolTip="화면을 캡처하여 클릭한 위치의 좌표를 가져옵니다."/>
                </Grid>

                                <!-- 2. Variable Mode Input -->
                                <Grid Margin="0,0,0,5">
                                    <Grid.Style>
                                        <Style TargetType="Grid">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding SourceType}" Value="Variable">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Grid.Style>
                                    
                                    <DockPanel LastChildFill="True">
                                        <TextBlock Text="좌표 변수:" VerticalAlignment="Center" Margin="0,0,10,0" Width="60"/>
                                        <components:VariableSelector ItemsSource="{Binding DataContext.AvailableCoordinateVariables, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                    Text="{Binding CoordinateVariableName, Mode=TwoWay}"
                                                                    DisplayMemberPath="Name"
                                                                    SelectedValuePath="Name"
                                                                    ToolTip="현재 그룹 및 상위 그룹에 정의된 좌표 변수 중 선택"/>
                                    </DockPanel>
                                </Grid>
                <!-- 3. Found Mode Info -->
                <TextBlock Text="ℹ️ 이전 조건(Pre-Condition)에서 찾은 위치를 클릭합니다." 
                           Foreground="#1565C0" Margin="0,0,0,5" FontStyle="Italic">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding SourceType}" Value="Found">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
                
                <DockPanel LastChildFill="True" Margin="0,0,0,5">
                    <TextBlock Text="클릭 종류:" VerticalAlignment="Center" Width="70"/>
                    <ComboBox SelectedValue="{Binding ClickType}" SelectedValuePath="Content">
                        <ComboBoxItem Content="Left"/><ComboBoxItem Content="Right"/><ComboBoxItem Content="Double"/>
                    </ComboBox>
                </DockPanel>

                <UniformGrid Columns="2" Margin="0,0,0,5">
                    <DockPanel Margin="0,0,5,0">
                        <TextBlock Text="클릭 횟수:" VerticalAlignment="Center" Width="70"/>
                        <TextBox Text="{Binding ClickCount, UpdateSourceTrigger=PropertyChanged}"/>
                    </DockPanel>
                    <DockPanel>
                        <TextBlock Text="간격(ms):" VerticalAlignment="Center" Width="70"/>
                        <TextBox Text="{Binding ClickIntervalMs, UpdateSourceTrigger=PropertyChanged}"/>
                    </DockPanel>
                </UniformGrid>

                <DockPanel>
                    <TextBlock Text="실패 시 이동:" VerticalAlignment="Center" Width="80" Foreground="Gray"/>
                    <components:JumpTargetSelector ItemsSource="{Binding DataContext.JumpTargets, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                  SelectedId="{Binding FailJumpId, Mode=TwoWay}"/>
                </DockPanel>
            </StackPanel>
        </DataTemplate>

        <!-- 3. KeyPressAction (Direct) -->
        <DataTemplate DataType="{x:Type models:KeyPressAction}">
            <StackPanel Margin="5">
                <DockPanel LastChildFill="True" Margin="0,0,0,5">
                    <TextBlock Text="키 코드:" VerticalAlignment="Center" Width="70"/>
                    <TextBox Text="{Binding KeyCode, UpdateSourceTrigger=PropertyChanged}" ToolTip="WPF Key Name (e.g. Enter, F1, A)"/>
                </DockPanel>
                <DockPanel LastChildFill="True" Margin="0,0,0,5">
                    <TextBlock Text="누름 시간:" VerticalAlignment="Center" Width="70"/>
                    <TextBox Text="{Binding PressDuration, UpdateSourceTrigger=PropertyChanged}" Width="50" HorizontalAlignment="Left" ToolTip="0 = Random Short Click"/>
                    <TextBlock Text="ms" VerticalAlignment="Center" Margin="5,0,0,0"/>
                </DockPanel>
                <DockPanel>
                    <TextBlock Text="실패 시 이동:" VerticalAlignment="Center" Width="80" Foreground="Gray"/>
                    <components:JumpTargetSelector ItemsSource="{Binding DataContext.JumpTargets, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                  SelectedId="{Binding FailJumpId, Mode=TwoWay}"/>
                </DockPanel>
            </StackPanel>
        </DataTemplate>

        <!-- 3-1. TextTypeAction (Direct) -->
        <DataTemplate DataType="{x:Type models:TextTypeAction}">
            <StackPanel Margin="5">
                <TextBlock Text="입력 소스 (Input Source):" Margin="0,0,0,5" Foreground="Gray"/>
                <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                    <RadioButton Content="직접 입력 (Direct)" GroupName="TextInputSrc" Margin="0,0,15,0"
                                 IsChecked="{Binding InputMode, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter=Direct}"/>
                    <RadioButton Content="변수값 (Variable)" GroupName="TextInputSrc"
                                 IsChecked="{Binding InputMode, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter=Variable}"
                                 ToolTip="변수에 저장된 값을 입력합니다."/>
                </StackPanel>

                <!-- Direct Mode Input -->
                <DockPanel LastChildFill="True" Margin="0,0,0,5">
                    <DockPanel.Style>
                        <Style TargetType="DockPanel">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding InputMode}" Value="Direct">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </DockPanel.Style>
                    <TextBlock Text="텍스트:" VerticalAlignment="Center" Width="70"/>
                    <TextBox Text="{Binding Text, UpdateSourceTrigger=PropertyChanged}" ToolTip="입력할 문자열 (Shift 자동 처리)"/>
                </DockPanel>

                <!-- Variable Mode Input -->
                <DockPanel LastChildFill="True" Margin="0,0,0,5">
                    <DockPanel.Style>
                        <Style TargetType="DockPanel">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding InputMode}" Value="Variable">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </DockPanel.Style>
                    <TextBlock Text="변수명:" VerticalAlignment="Center" Width="70"/>
                    <components:VariableSelector ItemsSource="{Binding DataContext.AvailableIntVariables, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                Text="{Binding VariableName, Mode=TwoWay}"
                                                ToolTip="입력할 값을 가진 변수를 선택하세요."/>
                </DockPanel>

                <DockPanel LastChildFill="True" Margin="0,0,0,5">
                    <TextBlock Text="입력 간격:" VerticalAlignment="Center" Width="70"/>
                    <TextBox Text="{Binding IntervalMs, UpdateSourceTrigger=PropertyChanged}" Width="50" HorizontalAlignment="Left"/>
                    <TextBlock Text="ms" VerticalAlignment="Center" Margin="5,0,0,0"/>
                </DockPanel>
                <DockPanel>
                    <TextBlock Text="실패 시 이동:" VerticalAlignment="Center" Width="80" Foreground="Gray"/>
                    <components:JumpTargetSelector ItemsSource="{Binding DataContext.JumpTargets, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                  SelectedId="{Binding FailJumpId, Mode=TwoWay}"/>
                </DockPanel>
            </StackPanel>
        </DataTemplate>

        <!-- 3-1. VariableSetAction (Direct) -->
        <DataTemplate DataType="{x:Type models:VariableSetAction}">
            <StackPanel Margin="5">
                <DockPanel LastChildFill="True" Margin="0,0,0,5">
                    <TextBlock Text="변수명:" VerticalAlignment="Center" Width="70"/>
                    <components:VariableSelector ItemsSource="{Binding DataContext.AvailableIntVariables, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                Text="{Binding VariableName, Mode=TwoWay}"/>
                </DockPanel>

                <Grid Margin="0,0,0,5">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="연산:" VerticalAlignment="Center" Width="70"/>
                    <ComboBox Grid.Column="1" SelectedValue="{Binding Operation}" SelectedValuePath="Content">
                        <ComboBoxItem Content="Set"/>
                        <ComboBoxItem Content="Add"/>
                        <ComboBoxItem Content="Sub"/>
                    </ComboBox>
                </Grid>

                <DockPanel LastChildFill="True" Margin="0,0,0,10">
                    <TextBlock Text="값:" VerticalAlignment="Center" Width="70"/>
                    <TextBox Text="{Binding Value, UpdateSourceTrigger=PropertyChanged}"/>
                </DockPanel>

                <DockPanel>
                    <TextBlock Text="실패 시 이동:" VerticalAlignment="Center" Width="80" Foreground="Gray"/>
                    <components:JumpTargetSelector ItemsSource="{Binding DataContext.JumpTargets, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                  SelectedId="{Binding FailJumpId, Mode=TwoWay}"/>
                </DockPanel>
            </StackPanel>
        </DataTemplate>

        <!-- 4. WindowControlAction (Direct) -->
        <DataTemplate DataType="{x:Type models:WindowControlAction}">
            <StackPanel Margin="5">
                <DockPanel LastChildFill="True" Margin="0,0,0,5">
                    <TextBlock Text="검색 기준:" VerticalAlignment="Center" Width="70"/>
                    <ComboBox ItemsSource="{Binding DataContext.SearchMethods, RelativeSource={RelativeSource AncestorType=UserControl}}"
                              SelectedItem="{Binding SearchMethod}"/>
                </DockPanel>

                <DockPanel LastChildFill="True" Margin="0,0,0,5">
                    <TextBlock Text="대상:" VerticalAlignment="Center" Width="70"/>
                    <Button DockPanel.Dock="Right" Content="⟳" Width="25" Margin="5,0,0,0"
                            Command="{Binding DataContext.RefreshTargetListCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                            CommandParameter="{Binding .}"
                            ToolTip="Refresh Target List"/>
                    <components:VariableSelector ItemsSource="{Binding DataContext.TargetList, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                Text="{Binding TargetName, Mode=TwoWay}"/>
                </DockPanel>

                <DockPanel>
                    <TextBlock Text="실패 시 이동:" VerticalAlignment="Center" Width="80" Foreground="Gray"/>
                    <components:JumpTargetSelector ItemsSource="{Binding DataContext.JumpTargets, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                  SelectedId="{Binding FailJumpId, Mode=TwoWay}"/>
                </DockPanel>
            </StackPanel>
        </DataTemplate>

        <!-- 5. MultiAction (Direct - Recursively uses Direct Templates) -->
        <DataTemplate DataType="{x:Type models:MultiAction}">
            <StackPanel Margin="5">
                <TextBlock Text="복합 액션 (Multi-Step Action)" FontWeight="Bold" Foreground="#673AB7" Margin="0,0,0,5"/>
                
                <Border Background="#F3E5F5" BorderBrush="#D1C4E9" BorderThickness="1" CornerRadius="4" Padding="5">
                    <StackPanel>
                        <!-- Sub-Actions List -->
                        <ItemsControl ItemsSource="{Binding Actions}">
                            <ItemsControl.Resources>
                                <!-- Force Direct Binding for children (Copy of Direct Templates) 
                                     This ensures sub-actions bind to their OWN FailJumpId, not the Proxy Property. 
                                     Since we are in Global Resources, the lookup works naturally. -->
                            </ItemsControl.Resources>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="White" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="2" Margin="0,0,0,5" Padding="5">
                                        <DockPanel>
                                            <!-- Delete Button -->
                                            <Button DockPanel.Dock="Right" Content="🗑" Width="25" Height="25" VerticalAlignment="Top" Margin="5,0,0,0"
                                                    Background="#FFEBEE" Foreground="#D32F2F" BorderThickness="0"
                                                    Command="{Binding DataContext.RemoveSubActionCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                    CommandParameter="{Binding .}"
                                                    ToolTip="Remove this sub-action"/>
                                            
                                            <!-- Action Content -->
                                            <ContentControl Content="{Binding}"/>
                                        </DockPanel>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>

                        <!-- Add Action Panel -->
                        <Separator Margin="0,5,0,5"/>
                        <DockPanel LastChildFill="True">
                            <Button DockPanel.Dock="Right" Content="+ 추가" Width="50"
                                    Command="{Binding DataContext.AddSubActionCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                    CommandParameter="{Binding .}"
                                    Background="#673AB7" Foreground="White" FontWeight="Bold"/>
                            <ComboBox ItemsSource="{Binding DataContext.ActionTypes, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                      SelectedItem="{Binding DataContext.SelectedSubActionType, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=TwoWay}"
                                      Margin="0,0,5,0"/>
                        </DockPanel>
                    </StackPanel>
                </Border>

                <DockPanel Margin="0,5,0,0">
                    <TextBlock Text="실패 시 이동:" VerticalAlignment="Center" Width="80" Foreground="Gray"/>
                    <components:JumpTargetSelector ItemsSource="{Binding DataContext.JumpTargets, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                  SelectedId="{Binding FailJumpId, Mode=TwoWay}"/>
                </DockPanel>
            </StackPanel>
        </DataTemplate>

    </UserControl.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="350"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Left: TreeView List -->
        <Grid Grid.Column="0" Background="#F0F0F0">
            <!-- ... (TreeView Logic Unchanged) ... -->
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Margin="10">
                <TextBlock Text="시퀀스 목록 (그룹/스텝)" FontWeight="Bold"/>
                <TextBlock Text="{Binding CurrentRecipeName, StringFormat='Recipe: {0}'}" FontSize="12" Foreground="Gray" Margin="0,2,0,0"/>
            </StackPanel>

            <TreeView Grid.Row="1" 
                      ItemsSource="{Binding Groups}"
                      Margin="5"
                      SelectedItemChanged="TreeView_SelectedItemChanged"
                      PreviewMouseLeftButtonDown="TreeView_PreviewMouseLeftButtonDown">
                <TreeView.ItemContainerStyle>
                    <Style TargetType="TreeViewItem">
                        <Setter Property="IsExpanded" Value="False"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsGroupStart}" Value="True">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding IsGroupEnd}" Value="True">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TreeView.ItemContainerStyle>
                
                <TreeView.Resources>
                    <HierarchicalDataTemplate DataType="{x:Type models:SequenceGroup}" ItemsSource="{Binding Nodes}">
                        <Grid Margin="0,2">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Margin="0,0,5,0">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Text" Value="[G]"/>
                                            <Setter Property="Foreground" Value="#FFA000"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsStartGroup}" Value="True">
                                                    <Setter Property="Text" Value="[START]"/>
                                                    <Setter Property="Foreground" Value="#D32F2F"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                                <TextBlock Text="{Binding Name}" FontWeight="Bold"/>
                            </StackPanel>
                            
                            <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="10,0,0,0">
                                <StackPanel.Style>
                                    <Style TargetType="StackPanel">
                                        <Setter Property="Visibility" Value="Visible"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsStartGroup}" Value="True">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </StackPanel.Style>
                                <Button Content="▲" Width="15" Height="15" Padding="0" Margin="0,0,2,0" FontSize="10"
                                        Command="{Binding DataContext.MoveGroupUpCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding .}" Background="Transparent" BorderThickness="0"/>
                                <Button Content="▼" Width="15" Height="15" Padding="0" FontSize="10"
                                        Command="{Binding DataContext.MoveGroupDownCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding .}" Background="Transparent" BorderThickness="0"/>
                            </StackPanel>
                        </Grid>
                    </HierarchicalDataTemplate>
                    
                    <DataTemplate DataType="{x:Type models:SequenceItem}">
                        <Grid Margin="0,2">
                            <Grid.Style>
                                <Style TargetType="Grid">
                                    <Setter Property="Visibility" Value="Visible"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsGroupStart}" Value="True">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding IsGroupEnd}" Value="True">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Grid.Style>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            
                            <CheckBox Grid.Column="0" IsChecked="{Binding IsEnabled}" VerticalAlignment="Center" Margin="0,0,5,0"/>
                            <TextBlock Grid.Column="1" Text="{Binding Name}" VerticalAlignment="Center"/>
                            
                            <StackPanel Grid.Column="2" Orientation="Horizontal" Margin="5,0,5,0">
                                <Button Content="▲" Width="15" Height="15" Padding="0" Margin="0,0,2,0" FontSize="10"
                                        Command="{Binding DataContext.MoveSequenceUpCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding .}" Background="Transparent" BorderThickness="0"/>
                                <Button Content="▼" Width="15" Height="15" Padding="0" FontSize="10"
                                        Command="{Binding DataContext.MoveSequenceDownCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding .}" Background="Transparent" BorderThickness="0"/>
                            </StackPanel>

                            <Button Grid.Column="3" Content="▶" Width="20" Height="20" Padding="0"
                                    Command="{Binding DataContext.RunSingleStepCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                    CommandParameter="{Binding .}"
                                    Background="Transparent" BorderThickness="0" Foreground="#2D8A3E" FontWeight="Bold"/>
                        </Grid>
                    </DataTemplate>
                </TreeView.Resources>
            </TreeView>

            <StackPanel Grid.Row="2" Orientation="Vertical" Margin="5">
                <WrapPanel HorizontalAlignment="Right" Margin="0,0,0,5">
                    <Button Content="그룹 추가" Command="{Binding AddGroupCommand}" Padding="10,5" Margin="0,0,5,0" Background="#FFB74D"/>
                    <Button Content="그룹 삭제" Command="{Binding RemoveGroupCommand}" Padding="10,5" Margin="0,0,5,0"/>
                    <Button Content="그룹 복사" Command="{Binding CopyGroupCommand}" Padding="10,5" Margin="0,0,5,0"/>
                    <Button Content="그룹 붙여넣기" Command="{Binding PasteGroupCommand}" Padding="10,5"/>
                </WrapPanel>
                <WrapPanel HorizontalAlignment="Right">
                    <Button Content="변수 관리" Command="{Binding OpenVariableManagerCommand}" Padding="10,5" Margin="0,0,5,0" Background="#E0E0E0"/>
                    <Button Content="스텝 추가" Command="{Binding AddSequenceCommand}" Padding="10,5" Margin="0,0,5,0"/>
                    <Button Content="스텝 삭제" Command="{Binding RemoveSequenceCommand}" Padding="10,5" Margin="0,0,15,0"/>
                    <Button Content="복사" Command="{Binding CopySequenceCommand}" Padding="8,5" Margin="0,0,5,0"/>
                    <Button Content="붙여넣기" Command="{Binding PasteSequenceCommand}" Padding="8,5" Margin="0,0,15,0"/>
                    <Button Content="저장" Command="{Binding SaveCommand}" Padding="15,5" Background="#4CAF50" Foreground="White" FontWeight="Bold"/>
                </WrapPanel>
            </StackPanel>
        </Grid>

        <!-- Right: Detail Editor -->
        <Grid Grid.Column="1" Margin="20">
            <!-- 1. Group Editor -->
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ScrollViewer.Style>
                    <Style TargetType="ScrollViewer">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding DataContext.SelectedSequence, RelativeSource={RelativeSource AncestorType=UserControl}}" Value="{x:Null}">
                                <DataTrigger.Setters>
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger.Setters>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding SelectedGroup}" Value="{x:Null}">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </ScrollViewer.Style>

                <StackPanel DataContext="{Binding SelectedGroup}">
                    <TextBlock Text="그룹 설정 (Group Settings)" FontSize="20" FontWeight="Bold" Margin="0,0,0,15" Foreground="#EF6C00">
                        <TextBlock.Style>
                             <Style TargetType="TextBlock">
                                 <Setter Property="Visibility" Value="Visible"/>
                                 <Style.Triggers>
                                     <DataTrigger Binding="{Binding IsStartGroup}" Value="True">
                                         <Setter Property="Visibility" Value="Collapsed"/>
                                     </DataTrigger>
                                 </Style.Triggers>
                             </Style>
                        </TextBlock.Style>
                    </TextBlock>

                    <TextBlock Text="시작 지점 설정 (Entry Point)" FontSize="20" FontWeight="Bold" Margin="0,0,0,15" Foreground="#D32F2F">
                        <TextBlock.Style>
                             <Style TargetType="TextBlock">
                                 <Setter Property="Visibility" Value="Collapsed"/>
                                 <Style.Triggers>
                                     <DataTrigger Binding="{Binding IsStartGroup}" Value="True">
                                         <Setter Property="Visibility" Value="Visible"/>
                                     </DataTrigger>
                                 </Style.Triggers>
                             </Style>
                        </TextBlock.Style>
                    </TextBlock>
                    
                    <GroupBox Header="시작 스텝 지정" Margin="0,0,0,15" Padding="10" BorderBrush="#D32F2F">
                        <GroupBox.Style>
                             <Style TargetType="GroupBox">
                                 <Setter Property="Visibility" Value="Collapsed"/>
                                 <Style.Triggers>
                                     <DataTrigger Binding="{Binding IsStartGroup}" Value="True">
                                         <Setter Property="Visibility" Value="Visible"/>
                                     </DataTrigger>
                                 </Style.Triggers>
                             </Style>
                        </GroupBox.Style>
                        
                        <StackPanel>
                            <TextBlock Text="매크로 실행 시 가장 먼저 수행할 스텝을 선택하세요." Margin="0,0,0,10" Foreground="Gray"/>
                            <DockPanel LastChildFill="True">
                                <TextBlock Text="시작 스텝:" VerticalAlignment="Center" Width="80" FontWeight="Bold"/>
                                <components:JumpTargetSelector ItemsSource="{Binding DataContext.JumpTargets, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                              SelectedId="{Binding StartJumpId, Mode=TwoWay}"/>
                            </DockPanel>
                        </StackPanel>
                    </GroupBox>

                    <StackPanel>
                        <StackPanel.Style>
                            <Style TargetType="StackPanel">
                                <Setter Property="Visibility" Value="Visible"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsStartGroup}" Value="True">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </StackPanel.Style>

                        <DockPanel LastChildFill="True" Margin="0,0,0,10">
                            <TextBlock Text="그룹 이름:" VerticalAlignment="Center" Margin="0,0,10,0" Width="80"/>
                            <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"/>
                        </DockPanel>

                        <GroupBox Header="그룹 타임아웃 (Timeout)" Margin="0,0,0,15" Padding="10" BorderBrush="#D32F2F">
                            <StackPanel>
                                <DockPanel LastChildFill="True" Margin="0,0,0,5">
                                    <TextBlock Text="제한 시간:" VerticalAlignment="Center" Width="110"/>
                                    <TextBox Text="{Binding TimeoutMs, UpdateSourceTrigger=PropertyChanged}" Width="60" DockPanel.Dock="Left"/>
                                    <TextBlock Text="ms (0 = 무제한)" VerticalAlignment="Center" Margin="5,0,0,0" Foreground="Gray"/>
                                </DockPanel>
                                <DockPanel LastChildFill="True">
                                    <TextBlock Text="시간 초과 시 이동:" VerticalAlignment="Center" Width="110" Foreground="#D32F2F"/>
                                    <components:JumpTargetSelector ItemsSource="{Binding DataContext.AvailableGroupExitTargets, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                  SelectedId="{Binding TimeoutJumpId, Mode=TwoWay}"
                                                                  ToolTip="제한 시간을 초과했을 때 이동할 스텝/그룹"/>
                                </DockPanel>
                            </StackPanel>
                        </GroupBox>

                        <GroupBox Header="그룹 흐름 제어 (Flow Control)" Margin="0,0,0,15" Padding="10" BorderBrush="#FF9800">
                            <StackPanel>
                                <DockPanel LastChildFill="True" Margin="0,0,0,5">
                                    <TextBlock Text="시작 스텝 (Entry):" VerticalAlignment="Center" Width="110"/>
                                    <components:JumpTargetSelector ItemsSource="{Binding DataContext.AvailableGroupEntryTargets, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                  SelectedId="{Binding DataContext.SelectedGroupEntryJumpId, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=TwoWay}"
                                                                  ToolTip="이 그룹 진입 시 처음 실행할 스텝"/>
                                </DockPanel>
                                <DockPanel LastChildFill="True">
                                    <TextBlock Text="종료 후 이동 (Next):" VerticalAlignment="Center" Width="110"/>
                                    <components:JumpTargetSelector ItemsSource="{Binding DataContext.AvailableGroupExitTargets, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                  SelectedId="{Binding DataContext.SelectedGroupExitJumpId, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=TwoWay}"
                                                                  ToolTip="이 그룹의 실행이 끝나면 이동할 다음 그룹"/>
                                </DockPanel>
                                                    </StackPanel>
                                                </GroupBox>
                            
                                                <!-- Group Post-Condition -->
                                                <GroupBox Header="완료 확인 및 조건부 분기 (Group Post-Condition)" Margin="0,0,0,15" Padding="10" BorderBrush="#673AB7">
                                                    <StackPanel>
                                                        <DockPanel LastChildFill="True" Margin="0,0,0,10">
                                                            <TextBlock Text="조건 유형:" VerticalAlignment="Center" Width="100"/>
                                                            <ComboBox ItemsSource="{Binding DataContext.ConditionTypes, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                                                      SelectedItem="{Binding DataContext.SelectedGroupPostConditionType, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=TwoWay}"/>
                                                        </DockPanel>
                            
                                                        <ContentControl Content="{Binding PostCondition}"
                                                                        BorderBrush="LightGray" BorderThickness="1" Padding="5" Margin="0,0,0,10">
                                                            <ContentControl.Resources>
                                                                <!-- Use Global Templates (Direct Binding) defined in UserControl.Resources -->
                                                                <!-- Unlike Step Editor, we don't need Proxy Binding here because we bind directly to Model properties -->
                                                                
                                                                <!-- But wait, Global Templates use JumpTargets (All Nodes). 
                                                                     We want Group Exit Targets (Siblings only). 
                                                                     So we MUST override templates to bind ComboBox ItemsSource to AvailableGroupExitTargets. -->
                                                                
                                                                <DataTemplate DataType="{x:Type models:ImageMatchCondition}">
                                                                    <StackPanel Margin="5">
                                                                        <DockPanel LastChildFill="True" Margin="0,0,0,5">
                                                                            <TextBlock Text="이미지:" VerticalAlignment="Center" Width="60"/>
                                                                            <StackPanel Orientation="Horizontal" DockPanel.Dock="Right">
                                                                                                                                <Button Content="파일 선택" Width="70" Margin="5,0,0,0" Command="{Binding DataContext.SelectImageCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding .}" ToolTip="Select existing image file"/>
                                                                                                                                <Button Content="화면 캡처" Width="70" Margin="5,0,0,0" Command="{Binding DataContext.CaptureImageCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding .}" ToolTip="Snip from screen"/>
                                                                                                                                <Button Content="Ref 갱신" Width="60" Margin="5,0,0,0" Command="{Binding DataContext.UpdateRefSizeCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding .}" ToolTip="현재 창 크기로 기준 해상도(Ref Size)를 갱신합니다." Background="#E0F7FA"/>
                                                                                                                            </StackPanel>                                                                            <TextBox Text="{Binding ImagePath, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Center" IsReadOnly="True"/>
                                                                        </DockPanel>
                                                                        <Border BorderBrush="#DDD" BorderThickness="1" Margin="0,0,0,5" Height="100" Background="#F8F8F8">
                                                                            <Image Source="{Binding ImagePath, Converter={StaticResource UriToBitmapConverter}}" Stretch="Uniform"/>
                                                                        </Border>
                                                                        <DockPanel LastChildFill="True" Margin="0,0,0,5">
                                                                            <TextBlock Text="일치율:" VerticalAlignment="Center" Width="100"/>
                                                                            <TextBlock Text="{Binding Threshold, StringFormat=P0}" DockPanel.Dock="Right" Width="40" TextAlignment="Right"/>
                                                                            <Slider Minimum="0.1" Maximum="1.0" Value="{Binding Threshold}" TickFrequency="0.1" IsSnapToTickEnabled="True"/>
                                                                        </DockPanel>
                                                                        <!-- Retry UI omitted for Group PostCondition simplicity or keep it? Keep it. -->
                                                                        <DockPanel>
                                                                            <TextBlock Text="실패 시 이동:" VerticalAlignment="Center" Width="80" Foreground="#C62828" FontWeight="Bold"/>
                                                                            <components:JumpTargetSelector ItemsSource="{Binding DataContext.AvailableGroupExitTargets, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                                                          SelectedId="{Binding DataContext.SelectedGroupPostConditionFailJumpId, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=TwoWay}"/>
                                                                        </DockPanel>
                                                                    </StackPanel>
                                                                </DataTemplate>
                            
                                                                <!-- Delay Condition -->
                                                                <DataTemplate DataType="{x:Type models:DelayCondition}">
                                                                    <StackPanel Margin="5">
                                                                        <Grid Margin="0,0,0,5">
                                                                            <Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                                                                            <TextBlock Text="대기 시간:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                                                            <TextBox Grid.Column="1" Text="{Binding DelayTimeMs, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Center"/>
                                                                            <TextBlock Grid.Column="2" Text="ms" VerticalAlignment="Center" Margin="5,0,0,0"/>
                                                                        </Grid>
                                                                        <DockPanel Margin="0,5,0,0">
                                                                            <TextBlock Text="실패 시 이동:" VerticalAlignment="Center" Width="80" Foreground="Gray"/>
                                                                            <components:JumpTargetSelector ItemsSource="{Binding DataContext.AvailableGroupExitTargets, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                                                          SelectedId="{Binding DataContext.SelectedGroupPostConditionFailJumpId, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=TwoWay}"/>
                                                                        </DockPanel>
                                                                    </StackPanel>
                                                                </DataTemplate>
                            
                                                                <!-- Process Running -->
                                                                <DataTemplate DataType="{x:Type models:ProcessRunningCondition}">
                                                                    <StackPanel Margin="5">
                                                                        <!-- Input Source Selection -->
                                                                        <TextBlock Text="입력 소스 (Input Source):" Margin="0,0,0,5" Foreground="Gray"/>
                                                                        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                                                                            <RadioButton Content="직접 입력 (Direct)" GroupName="GrpProcRunSrc" Margin="0,0,15,0"
                                                                                         IsChecked="{Binding SourceType, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter=Direct}"/>
                                                                            <RadioButton Content="변수값 (Variable)" GroupName="GrpProcRunSrc"
                                                                                         IsChecked="{Binding SourceType, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter=Variable}"
                                                                                         ToolTip="변수에 저장된 문자열을 대상 이름으로 사용합니다."/>
                                                                        </StackPanel>

                                                                        <DockPanel LastChildFill="True" Margin="0,0,0,10">
                                                                            <TextBlock Text="검색 기준:" VerticalAlignment="Center" Width="70"/>
                                                                            <ComboBox ItemsSource="{Binding DataContext.SearchMethods, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                                      SelectedItem="{Binding SearchMethod}"/>
                                                                        </DockPanel>

                                                                        <!-- Direct Mode Input -->
                                                                        <DockPanel LastChildFill="True" Margin="0,0,0,10">
                                                                            <DockPanel.Style>
                                                                                <Style TargetType="DockPanel">
                                                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                                                    <Style.Triggers>
                                                                                        <DataTrigger Binding="{Binding SourceType}" Value="Direct">
                                                                                            <Setter Property="Visibility" Value="Visible"/>
                                                                                        </DataTrigger>
                                                                                    </Style.Triggers>
                                                                                </Style>
                                                                            </DockPanel.Style>
                                                                            <TextBlock Text="대상 이름:" VerticalAlignment="Center" Width="70"/>
                                                                            <Button DockPanel.Dock="Right" Content="⟳" Width="25" Margin="5,0,0,0"
                                                                                    Command="{Binding DataContext.RefreshContextTargetCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                                    CommandParameter="{Binding .}"
                                                                                    ToolTip="Refresh Process List"/>
                                                                            <components:VariableSelector ItemsSource="{Binding DataContext.ProcessList, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                                                        Text="{Binding ProcessName, Mode=TwoWay}"
                                                                                                        ToolTip="Process Name or Window Title Part"/>
                                                                        </DockPanel>

                                                                        <!-- Variable Mode Input -->
                                                                        <DockPanel LastChildFill="True" Margin="0,0,0,10">
                                                                            <DockPanel.Style>
                                                                                <Style TargetType="DockPanel">
                                                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                                                    <Style.Triggers>
                                                                                        <DataTrigger Binding="{Binding SourceType}" Value="Variable">
                                                                                            <Setter Property="Visibility" Value="Visible"/>
                                                                                        </DataTrigger>
                                                                                    </Style.Triggers>
                                                                                </Style>
                                                                            </DockPanel.Style>
                                                                            <TextBlock Text="변수명:" VerticalAlignment="Center" Width="70"/>
                                                                            <components:VariableSelector ItemsSource="{Binding DataContext.AvailableIntVariables, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                                                        Text="{Binding ProcessNameVariable, Mode=TwoWay}"
                                                                                                        ToolTip="대상 이름을 담고 있는 변수를 선택하세요."/>
                                                                        </DockPanel>

                                                                        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                                                                            <TextBlock Text="조건 확인:" VerticalAlignment="Center" Width="70" Margin="0,0,5,0"/>
                                                                            <RadioButton Content="실행 중" GroupName="GrpProc" IsChecked="{Binding IsCheckRunning, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter=True}" Margin="0,0,15,0"/>
                                                                            <RadioButton Content="종료됨" GroupName="GrpProc" IsChecked="{Binding IsCheckRunning, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter=False}"/>
                                                                        </StackPanel>

                                                                        <UniformGrid Columns="2" Margin="0,0,0,10">
                                                                            <DockPanel Margin="0,0,5,0">
                                                                                <TextBlock Text="재시도 횟수:" VerticalAlignment="Center" Width="70"/>
                                                                                <TextBox Text="{Binding MaxSearchCount, UpdateSourceTrigger=PropertyChanged}" ToolTip="조건을 만족할 때까지 반복 시도할 최대 횟수입니다."/>
                                                                            </DockPanel>
                                                                            <DockPanel>
                                                                                <TextBlock Text="재시도 간격:" VerticalAlignment="Center" Width="70"/>
                                                                                <TextBox Text="{Binding SearchIntervalMs, UpdateSourceTrigger=PropertyChanged}" ToolTip="재시도 사이의 대기 시간(ms)입니다."/>
                                                                                <TextBlock Text="ms" VerticalAlignment="Center" Margin="5,0,0,0"/>
                                                                            </DockPanel>
                                                                        </UniformGrid>

                                                                        <DockPanel>
                                                                            <TextBlock Text="실패 시 이동:" VerticalAlignment="Center" Width="80" Foreground="#C62828" FontWeight="Bold"/>
                                                                            <components:JumpTargetSelector ItemsSource="{Binding DataContext.AvailableGroupExitTargets, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                                                          SelectedId="{Binding DataContext.SelectedGroupPostConditionFailJumpId, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=TwoWay}"/>
                                                                        </DockPanel>
                                                                    </StackPanel>
                                                                </DataTemplate>
                                                                
                                                                <!-- Variable Compare -->
                                                                <DataTemplate DataType="{x:Type models:VariableCompareCondition}">
                                                                    <StackPanel Margin="5">
                                                                        <DockPanel LastChildFill="True" Margin="0,0,0,5">
                                                                            <TextBlock Text="변수명:" VerticalAlignment="Center" Width="70"/>
                                                                            <components:VariableSelector ItemsSource="{Binding DataContext.AvailableIntVariables, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                                                        Text="{Binding DataContext.SelectedGroupPostConditionVariableName, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=TwoWay}"/>
                                                                        </DockPanel>
                                                                        <Grid Margin="0,0,0,5">
                                                                            <Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                                                                            <TextBlock Text="비교 연산:" VerticalAlignment="Center" Width="70"/>
                                                                            <ComboBox Grid.Column="1" SelectedValue="{Binding Operator}" SelectedValuePath="Content">
                                                                                <ComboBoxItem Content="=="/><ComboBoxItem Content="!="/><ComboBoxItem Content="&gt;"/><ComboBoxItem Content="&lt;"/><ComboBoxItem Content="&gt;="/><ComboBoxItem Content="&lt;="/><ComboBoxItem Content="Contains"/>
                                                                            </ComboBox>
                                                                        </Grid>
                                                                        
                                                                        <DockPanel LastChildFill="True" Margin="0,0,0,10">
                                                                            <TextBlock Text="비교값:" VerticalAlignment="Center" Width="70"/>
                                                                            <TextBox Text="{Binding TargetValue, UpdateSourceTrigger=PropertyChanged}"/>
                                                                        </DockPanel>

                                                                        <DockPanel>
                                                                            <TextBlock Text="조건 만족 시 이동 (True):" VerticalAlignment="Center" Width="140" Foreground="#2E7D32" FontWeight="Bold"
                                                                                       ToolTip="조건이 참(True)일 때 지정된 그룹으로 이동합니다. 거짓이면 다음 그룹으로 진행합니다."/>
                                                                            <components:JumpTargetSelector ItemsSource="{Binding DataContext.AvailableGroupExitTargets, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                                                          SelectedId="{Binding DataContext.SelectedGroupPostConditionFailJumpId, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=TwoWay}"/>
                                                                        </DockPanel>
                                                                    </StackPanel>
                                                                </DataTemplate>
                                                                
                                                                <!-- Switch Case (Group Version) -->
                                                                <DataTemplate DataType="{x:Type models:SwitchCaseCondition}">
                                                                    <StackPanel Margin="5">
                                                                        <DockPanel LastChildFill="True" Margin="0,0,0,5">
                                                                            <TextBlock Text="변수명:" VerticalAlignment="Center" Width="70"/>
                                                                            <components:VariableSelector ItemsSource="{Binding DataContext.AvailableIntVariables, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                                                        Text="{Binding DataContext.SelectedGroupPostConditionVariableName, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=TwoWay}"/>
                                                                        </DockPanel>

                                                                        <TextBlock Text="케이스 목록 (Cases):" FontWeight="Bold" Margin="0,5,0,5"/>
                                                                        
                                                                        <ItemsControl ItemsSource="{Binding DataContext.GroupPostConditionCases, RelativeSource={RelativeSource AncestorType=UserControl}}">
                                                                            <ItemsControl.ItemTemplate>
                                                                                <DataTemplate>
                                                                                    <Grid Margin="0,0,0,5">
                                                                                        <Grid.ColumnDefinitions>
                                                                                            <ColumnDefinition Width="Auto"/><ColumnDefinition Width="60"/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/>
                                                                                        </Grid.ColumnDefinitions>
                                                                                        <TextBlock Text="값(Int):" VerticalAlignment="Center" Margin="0,0,5,0"/>
                                                                                        <TextBox Grid.Column="1" Text="{Binding CaseValue, UpdateSourceTrigger=PropertyChanged}"/>
                                                                                        <TextBlock Grid.Column="2" Text="→ 이동:" VerticalAlignment="Center" Margin="10,0,5,0"/>
                                                                                        <components:JumpTargetSelector Grid.Column="3" 
                                                                                                                      ItemsSource="{Binding DataContext.AvailableGroupExitTargets, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                                                                      SelectedId="{Binding JumpId, Mode=TwoWay}"/>
                                                                                        <Button Grid.Column="4" Content="-" Width="25" Margin="5,0,0,0" Background="#FFEBEE" Foreground="Red"
                                                                                                Command="{Binding DataContext.RemoveSwitchCaseCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                                                CommandParameter="{Binding .}"/>
                                                                                    </Grid>
                                                                                </DataTemplate>
                                                                            </ItemsControl.ItemTemplate>
                                                                        </ItemsControl>

                                                                        <Button Content="+ 케이스 추가" 
                                                                                Command="{Binding DataContext.AddSwitchCaseCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                                CommandParameter="{Binding .}"
                                                                                Margin="0,5,0,10" Background="#E8F5E9"/>

                                                                        <DockPanel>
                                                                            <TextBlock Text="실패 시 이동:" VerticalAlignment="Center" Width="80" Foreground="#C62828" FontWeight="Bold"/>
                                                                            <components:JumpTargetSelector ItemsSource="{Binding DataContext.AvailableGroupExitTargets, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                                                          SelectedId="{Binding DataContext.SelectedGroupPostConditionFailJumpId, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=TwoWay}"/>
                                                                        </DockPanel>
                                                                    </StackPanel>
                                                                </DataTemplate>
                                                            </ContentControl.Resources>
                                                        </ContentControl>
                                                    </StackPanel>
                                                </GroupBox>
                            
                                                <GroupBox Header="대상 윈도우 설정 (Global Context for this Group)" Margin="0,0,0,15" Padding="10" BorderBrush="#9C27B0">                            <StackPanel>
                                <DockPanel LastChildFill="True" Margin="0,0,0,10">
                                    <TextBlock Text="좌표 모드:" VerticalAlignment="Center" Width="100"/>
                                    <ComboBox ItemsSource="{Binding DataContext.CoordinateModes, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                              SelectedItem="{Binding CoordinateMode}"/>
                                </DockPanel>

                                <StackPanel>
                                    <StackPanel.Style>
                                        <Style TargetType="StackPanel">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding CoordinateMode}" Value="WindowRelative">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </StackPanel.Style>
                                    
                                    <!-- Input Source Selection -->
                                    <TextBlock Text="입력 소스 (Input Source):" Margin="0,0,0,5" Foreground="Gray"/>
                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                                        <RadioButton Content="직접 입력 (Direct)" GroupName="GrpTargetSrc" Margin="0,0,15,0"
                                                     IsChecked="{Binding TargetNameSource, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter=Direct}"/>
                                        <RadioButton Content="변수값 (Variable)" GroupName="GrpTargetSrc"
                                                     IsChecked="{Binding TargetNameSource, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter=Variable}"
                                                     ToolTip="변수에 저장된 문자열을 대상 이름으로 사용합니다."/>
                                    </StackPanel>

                                    <DockPanel LastChildFill="True" Margin="0,0,0,5">
                                        <TextBlock Text="검색 기준:" VerticalAlignment="Center" Width="100"/>
                                        <ComboBox ItemsSource="{Binding DataContext.SearchMethods, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                  SelectedItem="{Binding ContextSearchMethod}"/>
                                    </DockPanel>

                                    <!-- Direct Mode Target Name -->
                                    <DockPanel LastChildFill="True" Margin="0,0,0,5">
                                        <DockPanel.Style>
                                            <Style TargetType="DockPanel">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding TargetNameSource}" Value="Direct">
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </DockPanel.Style>
                                        <TextBlock Text="대상 이름:" VerticalAlignment="Center" Width="100"/>
                                        <Button DockPanel.Dock="Right" Content="⟳" Width="25" Margin="5,0,0,0"
                                                Command="{Binding DataContext.RefreshContextTargetCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                CommandParameter="{Binding .}"
                                                ToolTip="목록 새로고침"/>
                                        <ComboBox IsEditable="True" 
                                                  ItemsSource="{Binding DataContext.ProcessList, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                  Text="{Binding TargetProcessName, UpdateSourceTrigger=PropertyChanged}"
                                                  ToolTip="Process Name or Window Title"/>
                                    </DockPanel>

                                    <!-- Variable Mode Target Name -->
                                    <DockPanel LastChildFill="True" Margin="0,0,0,5">
                                        <DockPanel.Style>
                                            <Style TargetType="DockPanel">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding TargetNameSource}" Value="Variable">
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </DockPanel.Style>
                                        <TextBlock Text="변수명:" VerticalAlignment="Center" Width="100"/>
                                        <components:VariableSelector ItemsSource="{Binding DataContext.AvailableIntVariables, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                    Text="{Binding TargetProcessNameVariable, Mode=TwoWay}"
                                                                    ToolTip="대상 이름을 담고 있는 변수를 선택하세요."/>
                                    </DockPanel>

                                    <DockPanel LastChildFill="True" Margin="0,0,0,5">
                                        <TextBlock Text="창 상태:" VerticalAlignment="Center" Width="100"/>
                                        <ComboBox ItemsSource="{Binding DataContext.WindowControlStates, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                  SelectedItem="{Binding ContextWindowState}"/>
                                    </DockPanel>

                                    <DockPanel LastChildFill="True" Margin="0,0,0,5">
                                        <TextBlock Text="미발견 시 이동:" VerticalAlignment="Center" Width="100" Foreground="#C62828"/>
                                        <components:JumpTargetSelector ItemsSource="{Binding DataContext.JumpTargets, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                      SelectedId="{Binding ProcessNotFoundJumpId, Mode=TwoWay}"/>
                                    </DockPanel>

                                    <UniformGrid Columns="2" Margin="0,5,0,0">
                                        <DockPanel Margin="0,0,10,0">
                                            <TextBlock Text="기준 너비:" VerticalAlignment="Center" Width="70"/>
                                            <TextBox Text="{Binding RefWindowWidth, UpdateSourceTrigger=PropertyChanged}"/>
                                        </DockPanel>
                                        <DockPanel>
                                            <TextBlock Text="기준 높이:" VerticalAlignment="Center" Width="70"/>
                                            <TextBox Text="{Binding RefWindowHeight, UpdateSourceTrigger=PropertyChanged}"/>
                                        </DockPanel>
                                    </UniformGrid>

                                    <Button Content="현재 대상 창 크기로 기준 해상도 설정" 
                                            Command="{Binding DataContext.UpdateRefSizeCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            CommandParameter="{Binding .}"
                                            Margin="0,5,0,5" Padding="5" Background="#E0F7FA" FontWeight="Bold"/>

                                    <!-- Warning for Zero Reference Size -->
                                    <TextBlock Text="⚠️ 기준 해상도가 0입니다. 스케일링이 작동하지 않습니다." 
                                               Foreground="Red" FontWeight="Bold" Margin="0,5,0,0" TextWrapping="Wrap">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                <Style.Triggers>
                                                    <MultiDataTrigger>
                                                        <MultiDataTrigger.Conditions>
                                                            <Condition Binding="{Binding RefWindowWidth}" Value="0"/>
                                                        </MultiDataTrigger.Conditions>
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                    </MultiDataTrigger>
                                                    <MultiDataTrigger>
                                                        <MultiDataTrigger.Conditions>
                                                            <Condition Binding="{Binding RefWindowHeight}" Value="0"/>
                                                        </MultiDataTrigger.Conditions>
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                    </MultiDataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </StackPanel>
                            </StackPanel>
                        </GroupBox>

                        <GroupBox Header="그룹 좌표 변수 (Coordinate Variables)" Margin="0,0,0,15" Padding="10" BorderBrush="#009688">
                            <StackPanel>
                                <DataGrid ItemsSource="{Binding Variables}" AutoGenerateColumns="False" CanUserAddRows="False" Height="150" Margin="0,0,0,5">
                                    <DataGrid.Columns>
                                        <DataGridTextColumn Header="이름 (Name)" Binding="{Binding Name}" Width="100"/>
                                        <DataGridTextColumn Header="X" Binding="{Binding X}" Width="50"/>
                                        <DataGridTextColumn Header="Y" Binding="{Binding Y}" Width="50"/>
                                        <DataGridTextColumn Header="설명" Binding="{Binding Description}" Width="*"/>
                                        <DataGridTemplateColumn Width="70">
                                            <DataGridTemplateColumn.CellTemplate>
                                                <DataTemplate>
                                                    <Button Content="📍 위치" 
                                                            Command="{Binding DataContext.PickVariableCoordinateCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                                            CommandParameter="{Binding .}" 
                                                            ToolTip="화면을 클릭하여 좌표 입력"/>
                                                </DataTemplate>
                                            </DataGridTemplateColumn.CellTemplate>
                                        </DataGridTemplateColumn>
                                        <DataGridTemplateColumn Width="40">
                                            <DataGridTemplateColumn.CellTemplate>
                                                <DataTemplate>
                                                    <Button Content="❌" 
                                                            Command="{Binding DataContext.RemoveCoordinateVariableCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                                            CommandParameter="{Binding .}" 
                                                            Foreground="Red" Background="Transparent" BorderThickness="0"/>
                                                </DataTemplate>
                                            </DataGridTemplateColumn.CellTemplate>
                                        </DataGridTemplateColumn>
                                    </DataGrid.Columns>
                                </DataGrid>
                                                            <Button Content="+ 변수 추가" 
                                                                    Command="{Binding DataContext.AddCoordinateVariableCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                                                    HorizontalAlignment="Right" Padding="10,5" Background="#E0F2F1"/>
                                                        </StackPanel>
                                                    </GroupBox>
                                
                                                    <GroupBox Header="그룹 정수 변수 (Integer Variables)" Margin="0,0,0,15" Padding="10" BorderBrush="#795548">
                                                        <StackPanel>
                                                            <DataGrid ItemsSource="{Binding IntVariables}" AutoGenerateColumns="False" CanUserAddRows="False" Height="150" Margin="0,0,0,5">
                                                                <DataGrid.Columns>
                                                                    <DataGridTextColumn Header="이름 (Name)" Binding="{Binding Name}" Width="100"/>
                                                                    <DataGridTextColumn Header="값 (Value)" Binding="{Binding Value}" Width="80"/>
                                                                    <DataGridTextColumn Header="설명" Binding="{Binding Description}" Width="*"/>
                                                                    <DataGridTemplateColumn Width="40">
                                                                        <DataGridTemplateColumn.CellTemplate>
                                                                            <DataTemplate>
                                                                                <Button Content="❌" 
                                                                                        Command="{Binding DataContext.RemoveIntVariableCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                                                                        CommandParameter="{Binding .}" 
                                                                                        Foreground="Red" Background="Transparent" BorderThickness="0"/>
                                                                            </DataTemplate>
                                                                        </DataGridTemplateColumn.CellTemplate>
                                                                    </DataGridTemplateColumn>
                                                                </DataGrid.Columns>
                                                            </DataGrid>
                                                            <Button Content="+ 변수 추가" 
                                                                    Command="{Binding DataContext.AddIntVariableCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                                                    HorizontalAlignment="Right" Padding="10,5" Background="#EFEBE9"/>
                                                        </StackPanel>
                                                    </GroupBox>
                                                </StackPanel>
                                            </StackPanel>            </ScrollViewer>

            <!-- 2. Step Editor -->
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Grid.Style>
                    <Style TargetType="Grid">
                        <Setter Property="Visibility" Value="Visible"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding SelectedSequence}" Value="{x:Null}">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Grid.Style>

                <!-- Header -->
                <StackPanel Grid.Row="0" DataContext="{Binding SelectedSequence}">
                    <TextBlock Text="단계 상세 설정 (Step Settings)" FontSize="20" FontWeight="Bold" Margin="0,0,0,10"/>
                    <DockPanel LastChildFill="True" Margin="0,0,0,10">
                        <TextBlock Text="이름:" VerticalAlignment="Center" Margin="0,0,10,0" Width="50"/>
                        <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"/>
                    </DockPanel>

                    <UniformGrid Columns="3" Margin="0,0,0,20">
                        <DockPanel Margin="0,0,10,0">
                            <TextBlock Text="반복:" VerticalAlignment="Center" Margin="0,0,5,0" Width="50"/>
                            <TextBox Text="{Binding RepeatCount, UpdateSourceTrigger=PropertyChanged}"/>
                        </DockPanel>
                        <DockPanel Margin="0,0,10,0">
                            <TextBlock Text="재시도:" VerticalAlignment="Center" Margin="0,0,5,0" Width="40"/>
                            <TextBox Text="{Binding RetryCount, UpdateSourceTrigger=PropertyChanged}"/>
                        </DockPanel>
                        <DockPanel>
                            <TextBlock Text="지연:" VerticalAlignment="Center" Margin="0,0,5,0" Width="40"/>
                            <TextBox Text="{Binding RetryDelayMs, UpdateSourceTrigger=PropertyChanged}" ToolTip="Retry delay in milliseconds"/>
                            <TextBlock Text="ms" VerticalAlignment="Center" Margin="5,0,0,0"/>
                        </DockPanel>
                    </UniformGrid>
                </StackPanel>

                <!-- ScrollViewer for 3-Step Edit -->
                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <!-- Step 1: Pre-Condition -->
                        <GroupBox Header="1. 시작 조건 (Pre-Condition)" Margin="0,0,0,15" Padding="10">
                            <StackPanel>
                                <DockPanel LastChildFill="True" Margin="0,0,0,10">
                                    <TextBlock Text="조건 유형:" VerticalAlignment="Center" Width="100"/>
                                    <ComboBox ItemsSource="{Binding ConditionTypes}" 
                                              SelectedItem="{Binding SelectedPreConditionType, Mode=TwoWay}"/>
                                </DockPanel>
                                
                                <ContentControl Content="{Binding SelectedSequence.PreCondition}"
                                                BorderBrush="LightGray" BorderThickness="1" Padding="5">
                                    <ContentControl.Resources>
                                        <!-- PROXY BINDING for SwitchCaseCondition (Pre) -->
                                        <DataTemplate DataType="{x:Type models:SwitchCaseCondition}">
                                            <StackPanel Margin="5">
                                                <DockPanel LastChildFill="True" Margin="0,0,0,5">
                                                    <TextBlock Text="변수명:" VerticalAlignment="Center" Width="70"/>
                                                    <components:VariableSelector ItemsSource="{Binding DataContext.AvailableIntVariables, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                                Text="{Binding TargetVariableName, Mode=TwoWay}"/>
                                                </DockPanel>

                                                <TextBlock Text="케이스 목록 (Cases):" FontWeight="Bold" Margin="0,5,0,5"/>
                                                
                                                <ItemsControl ItemsSource="{Binding DataContext.StepPreSwitchCases, RelativeSource={RelativeSource AncestorType=UserControl}}">
                                                    <ItemsControl.ItemTemplate>
                                                        <DataTemplate>
                                                            <Grid Margin="0,0,0,5">
                                                                <Grid.ColumnDefinitions>
                                                                    <ColumnDefinition Width="Auto"/><ColumnDefinition Width="60"/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/>
                                                                </Grid.ColumnDefinitions>
                                                                <TextBlock Text="값(Int):" VerticalAlignment="Center" Margin="0,0,5,0"/>
                                                                <TextBox Grid.Column="1" Text="{Binding CaseValue, UpdateSourceTrigger=PropertyChanged}"/>
                                                                <TextBlock Grid.Column="2" Text="→ 이동:" VerticalAlignment="Center" Margin="10,0,5,0"/>
                                                                <components:JumpTargetSelector Grid.Column="3" 
                                                                                              ItemsSource="{Binding DataContext.JumpTargets, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                                              SelectedId="{Binding JumpId, Mode=TwoWay}"/>
                                                                <Button Grid.Column="4" Content="-" Width="25" Margin="5,0,0,0" Background="#FFEBEE" Foreground="Red"
                                                                        Command="{Binding DataContext.RemoveSwitchCaseCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                        CommandParameter="{Binding .}"/>
                                                            </Grid>
                                                        </DataTemplate>
                                                    </ItemsControl.ItemTemplate>
                                                </ItemsControl>

                                                <Button Content="+ 케이스 추가" 
                                                        Command="{Binding DataContext.AddSwitchCaseCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                        CommandParameter="{Binding .}"
                                                        Margin="0,5,0,10" Background="#E8F5E9"/>

                                                <DockPanel>
                                                    <TextBlock Text="실패 시 이동:" VerticalAlignment="Center" Width="80" Foreground="#C62828" FontWeight="Bold"/>
                                                    <components:JumpTargetSelector ItemsSource="{Binding DataContext.JumpTargets, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                                  SelectedId="{Binding DataContext.SelectedStepPreConditionFailJumpId, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=TwoWay}"/>
                                                </DockPanel>
                                            </StackPanel>
                                        </DataTemplate>

                                        <!-- PROXY BINDING for Pre-Condition -->
                                        <DataTemplate DataType="{x:Type models:ImageMatchCondition}">
                                            <StackPanel Margin="5">
                                                <DockPanel LastChildFill="True" Margin="0,0,0,5">
                                                    <TextBlock Text="이미지:" VerticalAlignment="Center" Width="60"/>
                                                    <StackPanel Orientation="Horizontal" DockPanel.Dock="Right">
                                                                                                        <Button Content="파일 선택" Width="70" Margin="5,0,0,0" Command="{Binding DataContext.SelectImageCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding .}" ToolTip="Select existing image file"/>
                                                                                                        <Button Content="화면 캡처" Width="70" Margin="5,0,0,0" Command="{Binding DataContext.CaptureImageCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding .}" ToolTip="Snip from screen"/>
                                                                                                        <Button Content="Ref 갱신" Width="60" Margin="5,0,0,0" Command="{Binding DataContext.UpdateRefSizeCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding .}" ToolTip="현재 창 크기로 기준 해상도(Ref Size)를 갱신합니다." Background="#E0F7FA"/>
                                                                                                    </StackPanel>                                                    <TextBox Text="{Binding ImagePath, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Center" IsReadOnly="True"/>
                                                </DockPanel>
                                                <Border BorderBrush="#DDD" BorderThickness="1" Margin="0,0,0,5" Height="100" Background="#F8F8F8">
                                                    <Image Source="{Binding ImagePath, Converter={StaticResource UriToBitmapConverter}}" Stretch="Uniform"/>
                                                </Border>
                                                <DockPanel LastChildFill="True" Margin="0,0,0,5">
                                                    <TextBlock Text="일치율:" VerticalAlignment="Center" Width="100"/>
                                                    <TextBlock Text="{Binding Threshold, StringFormat=P0}" DockPanel.Dock="Right" Width="40" TextAlignment="Right"/>
                                                    <Slider Minimum="0.1" Maximum="1.0" Value="{Binding Threshold}" TickFrequency="0.1" IsSnapToTickEnabled="True"/>
                                                </DockPanel>

                                                <UniformGrid Columns="2" Margin="0,0,0,5">
                                                    <DockPanel Margin="0,0,5,0">
                                                        <TextBlock Text="재시도 횟수:" VerticalAlignment="Center" Width="70"/>
                                                        <TextBox Text="{Binding MaxSearchCount, UpdateSourceTrigger=PropertyChanged}" ToolTip="이미지를 찾을 때까지 반복 시도할 최대 횟수입니다."/>
                                                    </DockPanel>
                                                    <DockPanel>
                                                        <TextBlock Text="재시도 간격:" VerticalAlignment="Center" Width="70"/>
                                                        <TextBox Text="{Binding SearchIntervalMs, UpdateSourceTrigger=PropertyChanged}" ToolTip="재시도 사이의 대기 시간(ms)입니다."/>
                                                        <TextBlock Text="ms" VerticalAlignment="Center" Margin="5,0,0,0"/>
                                                    </DockPanel>
                                                </UniformGrid>

                                                <!-- Test Section -->
                                                <Border Background="#E3F2FD" CornerRadius="4" Padding="8" Margin="0,0,0,10">
                                                    <StackPanel>
                                                        <Grid Margin="0,0,0,5">
                                                            <Grid.ColumnDefinitions>
                                                                <ColumnDefinition Width="*"/>
                                                                <ColumnDefinition Width="Auto"/>
                                                            </Grid.ColumnDefinitions>
                                                            <StackPanel>
                                                                <StackPanel Orientation="Horizontal" Margin="0,0,0,2">
                                                                    <TextBlock Text="Match Score: " FontWeight="Bold"/>
                                                                    <TextBlock Text="{Binding TestScore, StringFormat=P1}" Foreground="#1565C0" FontWeight="Bold"/>
                                                                </StackPanel>
                                                                <StackPanel Orientation="Horizontal">
                                                                    <TextBlock Text="Result: " FontWeight="Bold"/>
                                                                    <TextBlock Text="{Binding TestResult}" Foreground="#0D47A1"/>
                                                                </StackPanel>
                                                            </StackPanel>
                                                            <Button Grid.Column="1" Content="🔍 Test Match" 
                                                                    Padding="10,5" Background="#2196F3" Foreground="White" FontWeight="Bold"
                                                                    Command="{Binding DataContext.TestImageConditionCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                    CommandParameter="{Binding .}"/>
                                                        </Grid>
                                                        
                                                        <!-- Test Result Image Preview -->
                                                        <Border BorderBrush="#90CAF9" BorderThickness="1" Background="White" Margin="0,5,0,0">
                                                            <Border.Style>
                                                                <Style TargetType="Border">
                                                                    <Setter Property="Visibility" Value="Visible"/>
                                                                    <Style.Triggers>
                                                                        <DataTrigger Binding="{Binding DataContext.TestResultImage, RelativeSource={RelativeSource AncestorType=UserControl}}" Value="{x:Null}">
                                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                                        </DataTrigger>
                                                                    </Style.Triggers>
                                                                </Style>
                                                            </Border.Style>
                                                            <Image Source="{Binding DataContext.TestResultImage, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                                                   MaxHeight="400" Stretch="Uniform" ToolTip="Matching Result (Red: Found, Blue: ROI)"/>
                                                        </Border>
                                                    </StackPanel>
                                                </Border>

                                                <!-- ROI Settings (Restored) -->
                                                <GroupBox Header="검색 영역 (ROI)" Margin="0,5,0,10">
                                                    <StackPanel>
                                                        <CheckBox Content="영역 지정 사용 (ROI)" IsChecked="{Binding UseRegion}" Margin="0,0,0,5"/>
                                                        <Grid IsEnabled="{Binding UseRegion}">
                                                            <Grid.ColumnDefinitions>
                                                                <ColumnDefinition Width="*"/>
                                                                <ColumnDefinition Width="Auto"/>
                                                            </Grid.ColumnDefinitions>
                                                            
                                                            <StackPanel Orientation="Vertical">
                                                                <UniformGrid Columns="2" Margin="0,0,0,5">
                                                                    <DockPanel Margin="0,0,5,0">
                                                                        <TextBlock Text="X:" VerticalAlignment="Center" Width="20" FontWeight="Bold"/>
                                                                        <TextBox Text="{Binding RegionX, UpdateSourceTrigger=PropertyChanged}"/>
                                                                    </DockPanel>
                                                                    <DockPanel>
                                                                        <TextBlock Text="Y:" VerticalAlignment="Center" Width="20" FontWeight="Bold"/>
                                                                        <TextBox Text="{Binding RegionY, UpdateSourceTrigger=PropertyChanged}"/>
                                                                    </DockPanel>
                                                                </UniformGrid>
                                                                <UniformGrid Columns="2">
                                                                    <DockPanel Margin="0,0,5,0">
                                                                        <TextBlock Text="W:" VerticalAlignment="Center" Width="20" FontWeight="Bold"/>
                                                                        <TextBox Text="{Binding RegionW, UpdateSourceTrigger=PropertyChanged}"/>
                                                                    </DockPanel>
                                                                    <DockPanel>
                                                                        <TextBlock Text="H:" VerticalAlignment="Center" Width="20" FontWeight="Bold"/>
                                                                        <TextBox Text="{Binding RegionH, UpdateSourceTrigger=PropertyChanged}"/>
                                                                    </DockPanel>
                                                                </UniformGrid>
                                                            </StackPanel>
                                                            
                                                            <Button Grid.Column="1" Content="영역 선택" 
                                                                    Margin="10,0,0,0" Padding="10,0"
                                                                    Command="{Binding DataContext.PickRegionCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                    CommandParameter="{Binding .}"/>
                                                        </Grid>
                                                    </StackPanel>
                                                </GroupBox>

                                                <!-- PROXY BINDING HERE -->
                                                <DockPanel>
                                                    <TextBlock Text="실패 시 이동:" VerticalAlignment="Center" Width="80" Foreground="#C62828" FontWeight="Bold"/>
                                                    <components:JumpTargetSelector ItemsSource="{Binding DataContext.JumpTargets, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                                  SelectedId="{Binding DataContext.SelectedStepPreConditionFailJumpId, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=TwoWay}"/>
                                                </DockPanel>
                                            </StackPanel>
                                        </DataTemplate>

                                                                                                                        <!-- Proxy Binding for ProcessRunningCondition (Pre) -->

                                                                                                                        <DataTemplate DataType="{x:Type models:ProcessRunningCondition}">

                                                                                                                            <StackPanel Margin="5">

                                                                                                                                <!-- Input Source Selection -->

                                                                                                                                <TextBlock Text="입력 소스 (Input Source):" Margin="0,0,0,5" Foreground="Gray"/>

                                                                                                                                <StackPanel Orientation="Horizontal" Margin="0,0,0,10">

                                                                                                                                    <RadioButton Content="직접 입력 (Direct)" GroupName="ProcRunPreSrc" Margin="0,0,15,0"

                                                                                                                                                 IsChecked="{Binding SourceType, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter=Direct}"/>

                                                                                                                                    <RadioButton Content="변수값 (Variable)" GroupName="ProcRunPreSrc"

                                                                                                                                                 IsChecked="{Binding SourceType, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter=Variable}"

                                                                                                                                                 ToolTip="변수에 저장된 문자열을 대상 이름으로 사용합니다."/>

                                                                                                                                </StackPanel>

                                                                                

                                                                                                                                <DockPanel LastChildFill="True" Margin="0,0,0,10">

                                                                                                                                    <TextBlock Text="검색 기준:" VerticalAlignment="Center" Width="70"/>

                                                                                                                                    <ComboBox ItemsSource="{Binding DataContext.SearchMethods, RelativeSource={RelativeSource AncestorType=UserControl}}"

                                                                                                                                              SelectedItem="{Binding SearchMethod}"/>

                                                                                                                                </DockPanel>

                                                                                

                                                                                                                                <!-- Direct Mode Input -->

                                                                                                                                <DockPanel LastChildFill="True" Margin="0,0,0,10">

                                                                                                                                    <DockPanel.Style>

                                                                                                                                        <Style TargetType="DockPanel">

                                                                                                                                            <Setter Property="Visibility" Value="Collapsed"/>

                                                                                                                                            <Style.Triggers>

                                                                                                                                                <DataTrigger Binding="{Binding SourceType}" Value="Direct">

                                                                                                                                                    <Setter Property="Visibility" Value="Visible"/>

                                                                                                                                                </DataTrigger>

                                                                                                                                            </Style.Triggers>

                                                                                                                                        </Style>

                                                                                                                                    </DockPanel.Style>

                                                                                                                                    <TextBlock Text="대상 이름:" VerticalAlignment="Center" Width="70"/>

                                                                                                                                    <Button DockPanel.Dock="Right" Content="⟳" Width="25" Margin="5,0,0,0"

                                                                                                                                            Command="{Binding DataContext.RefreshContextTargetCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"

                                                                                                                                            CommandParameter="{Binding .}"

                                                                                                                                            ToolTip="Refresh Process List"/>

                                                                                                                                    <components:VariableSelector ItemsSource="{Binding DataContext.ProcessList, RelativeSource={RelativeSource AncestorType=UserControl}}"

                                                                                                                                                                Text="{Binding ProcessName, Mode=TwoWay}"

                                                                                                                                                                ToolTip="Process Name or Window Title Part"/>

                                                                                                                                </DockPanel>

                                                                                

                                                                                                                                <!-- Variable Mode Input -->

                                                                                                                                <DockPanel LastChildFill="True" Margin="0,0,0,10">

                                                                                                                                    <DockPanel.Style>

                                                                                                                                        <Style TargetType="DockPanel">

                                                                                                                                            <Setter Property="Visibility" Value="Collapsed"/>

                                                                                                                                            <Style.Triggers>

                                                                                                                                                <DataTrigger Binding="{Binding SourceType}" Value="Variable">

                                                                                                                                                    <Setter Property="Visibility" Value="Visible"/>

                                                                                                                                                </DataTrigger>

                                                                                                                                            </Style.Triggers>

                                                                                                                                        </Style>

                                                                                                                                    </DockPanel.Style>

                                                                                                                                    <TextBlock Text="변수명:" VerticalAlignment="Center" Width="70"/>

                                                                                                                                    <components:VariableSelector ItemsSource="{Binding DataContext.AvailableIntVariables, RelativeSource={RelativeSource AncestorType=UserControl}}"

                                                                                                                                                                Text="{Binding ProcessNameVariable, Mode=TwoWay}"

                                                                                                                                                                ToolTip="대상 이름을 담고 있는 변수를 선택하세요."/>

                                                                                                                                </DockPanel>                                                <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                                                    <TextBlock Text="조건 확인:" VerticalAlignment="Center" Width="70" Margin="0,0,5,0"/>
                                                    <RadioButton Content="실행 중 (Running)" GroupName="ProcRunPre" 
                                                                 IsChecked="{Binding IsCheckRunning, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter=True}" 
                                                                 Margin="0,0,15,0"/>
                                                    <RadioButton Content="종료됨 (Not Running)" GroupName="ProcRunPre"
                                                                 IsChecked="{Binding IsCheckRunning, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter=False}"/>
                                                </StackPanel>

                                                <UniformGrid Columns="2" Margin="0,0,0,10">
                                                    <DockPanel Margin="0,0,5,0">
                                                        <TextBlock Text="재시도 횟수:" VerticalAlignment="Center" Width="70"/>
                                                        <TextBox Text="{Binding MaxSearchCount, UpdateSourceTrigger=PropertyChanged}" ToolTip="조건을 만족할 때까지 반복 시도할 최대 횟수입니다."/>
                                                    </DockPanel>
                                                    <DockPanel>
                                                        <TextBlock Text="재시도 간격:" VerticalAlignment="Center" Width="70"/>
                                                        <TextBox Text="{Binding SearchIntervalMs, UpdateSourceTrigger=PropertyChanged}" ToolTip="재시도 사이의 대기 시간(ms)입니다."/>
                                                        <TextBlock Text="ms" VerticalAlignment="Center" Margin="5,0,0,0"/>
                                                    </DockPanel>
                                                </UniformGrid>

                                                <DockPanel>
                                                    <TextBlock Text="실패 시 이동:" VerticalAlignment="Center" Width="80" Foreground="#C62828" FontWeight="Bold"/>
                                                    <components:JumpTargetSelector ItemsSource="{Binding DataContext.JumpTargets, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                                  SelectedId="{Binding DataContext.SelectedStepPreConditionFailJumpId, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=TwoWay}"/>
                                                </DockPanel>
                                            </StackPanel>
                                        </DataTemplate>
                                        <!-- Add other proxy templates for Delay, Gray, etc. if needed. Assuming ImageMatch is primary target. -->
                                    </ContentControl.Resources>
                                </ContentControl>
                            </StackPanel>
                        </GroupBox>

                        <!-- Step 2: Action -->
                        <GroupBox Header="2. 동작 (Action)" Margin="0,0,0,15" Padding="10" BorderBrush="#2196F3">
                            <StackPanel>
                                <DockPanel LastChildFill="True" Margin="0,0,0,10">
                                    <TextBlock Text="동작 유형:" VerticalAlignment="Center" Width="100"/>
                                    <ComboBox ItemsSource="{Binding ActionTypes}" 
                                              SelectedItem="{Binding SelectedActionType, Mode=TwoWay}"/>
                                </DockPanel>

                                <ContentControl Content="{Binding SelectedSequence.Action}"
                                                BorderBrush="LightGray" BorderThickness="1" Padding="5">
                                    <ContentControl.Resources>
                                        <!-- Add Converter Locally for Lookup Safety -->
                                        <utils:EnumToBooleanConverter x:Key="EnumToBooleanConverter"/>
                                        
                                        <!-- PROXY BINDING for Action (e.g. MouseClick) -->
                                        <DataTemplate DataType="{x:Type models:MouseClickAction}">
                                            <StackPanel Margin="5">
                                                <!-- Source Selection -->
                                                <TextBlock Text="좌표 소스 (Coordinate Source):" Margin="0,0,0,5" Foreground="Gray"/>
                                                <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                                                    <RadioButton Content="지정 좌표 (Fixed)" GroupName="CoordSourceLocal" Margin="0,0,15,0"
                                                                 IsChecked="{Binding SourceType, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter=Fixed}"/>
                                                    <RadioButton Content="찾은 좌표 (Found)" GroupName="CoordSourceLocal" Margin="0,0,15,0"
                                                                 IsChecked="{Binding SourceType, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter=Found}"
                                                                 ToolTip="Pre-Condition(이미지/Gray)에서 찾은 위치를 사용합니다."/>
                                                    <RadioButton Content="변수 좌표 (Var)" GroupName="CoordSourceLocal"
                                                                 IsChecked="{Binding SourceType, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter=Variable}"
                                                                 ToolTip="변수에 저장된 값을 좌표로 사용합니다."/>
                                                </StackPanel>

                                                <!-- 1. Fixed Mode Input -->
                                                <Grid Margin="0,0,0,5">
                                                    <Grid.Style>
                                                        <Style TargetType="Grid">
                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding SourceType}" Value="Fixed">
                                                                    <Setter Property="Visibility" Value="Visible"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </Grid.Style>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>
                                                    <TextBlock Text="X:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                                                    <TextBox Grid.Column="1" Text="{Binding X, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,10,0"/>
                                                    <TextBlock Grid.Column="2" Text="Y:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                                                    <TextBox Grid.Column="3" Text="{Binding Y, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,10,0"/>
                                                    <Button Grid.Column="4" Content="현재위치" Padding="5,0" 
                                                            Command="{Binding DataContext.PickCoordinateCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                            CommandParameter="{Binding .}"
                                                            ToolTip="화면을 캡처하여 클릭한 위치의 좌표를 가져옵니다."/>
                                                </Grid>

                                                                <!-- 2. Variable Mode Input -->
                                                                <Grid Margin="0,0,0,5">
                                                                    <Grid.Style>
                                                                        <Style TargetType="Grid">
                                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                                            <Style.Triggers>
                                                                                <DataTrigger Binding="{Binding SourceType}" Value="Variable">
                                                                                    <Setter Property="Visibility" Value="Visible"/>
                                                                                </DataTrigger>
                                                                            </Style.Triggers>
                                                                        </Style>
                                                                    </Grid.Style>
                                                                    
                                                                                                        <DockPanel LastChildFill="True">
                                                                    
                                                                                                            <TextBlock Text="좌표 변수:" VerticalAlignment="Center" Margin="0,0,10,0" Width="60"/>
                                                                    
                                                                                                            <components:VariableSelector ItemsSource="{Binding DataContext.AvailableCoordinateVariables, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                    
                                                                                                                                        Text="{Binding CoordinateVariableName, Mode=TwoWay}"
                                                                    
                                                                                                                                        DisplayMemberPath="Name"
                                                                    
                                                                                                                                        SelectedValuePath="Name"
                                                                    
                                                                                                                                        ToolTip="현재 그룹 및 상위 그룹에 정의된 좌표 변수 중 선택"/>
                                                                    
                                                                                                        </DockPanel>
                                                                    
                                                                    
                                                                </Grid>
                                                <!-- 3. Found Mode Info -->
                                                <TextBlock Text="ℹ️ 이전 조건(Pre-Condition)에서 찾은 위치를 클릭합니다." 
                                                           Foreground="#1565C0" Margin="0,0,0,5" FontStyle="Italic">
                                                    <TextBlock.Style>
                                                        <Style TargetType="TextBlock">
                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding SourceType}" Value="Found">
                                                                    <Setter Property="Visibility" Value="Visible"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </TextBlock.Style>
                                                </TextBlock>

                                                <DockPanel LastChildFill="True" Margin="0,0,0,5">
                                                    <TextBlock Text="클릭 종류:" VerticalAlignment="Center" Width="70"/>
                                                    <ComboBox SelectedValue="{Binding ClickType}" SelectedValuePath="Content">
                                                        <ComboBoxItem Content="Left"/><ComboBoxItem Content="Right"/><ComboBoxItem Content="Double"/>
                                                    </ComboBox>
                                                </DockPanel>
                                                <UniformGrid Columns="2" Margin="0,0,0,5">
                                                    <DockPanel Margin="0,0,5,0">
                                                        <TextBlock Text="클릭 횟수:" VerticalAlignment="Center" Width="70"/>
                                                        <TextBox Text="{Binding ClickCount, UpdateSourceTrigger=PropertyChanged}"/>
                                                    </DockPanel>
                                                    <DockPanel>
                                                        <TextBlock Text="간격(ms):" VerticalAlignment="Center" Width="70"/>
                                                        <TextBox Text="{Binding ClickIntervalMs, UpdateSourceTrigger=PropertyChanged}"/>
                                                    </DockPanel>
                                                </UniformGrid>
                                                <!-- PROXY BINDING HERE -->
                                                <DockPanel>
                                                    <TextBlock Text="실패 시 이동:" VerticalAlignment="Center" Width="80" Foreground="Gray"/>
                                                    <components:JumpTargetSelector ItemsSource="{Binding DataContext.JumpTargets, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                                  SelectedId="{Binding DataContext.SelectedStepActionFailJumpId, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=TwoWay}"/>
                                                </DockPanel>
                                            </StackPanel>
                                        </DataTemplate>
                                        <!-- Add other proxy templates for Idle, KeyPress, etc. -->
                                    </ContentControl.Resources>
                                </ContentControl>
                            </StackPanel>
                        </GroupBox>

                        <!-- Step 3: Post-Condition & Flow Control -->
                        <GroupBox Header="3. 완료 확인 및 흐름 제어" Margin="0,0,0,15" Padding="10">
                            <StackPanel>
                                <DockPanel LastChildFill="True" Margin="0,0,0,10">
                                    <TextBlock Text="조건 유형:" VerticalAlignment="Center" Width="100"/>
                                    <ComboBox ItemsSource="{Binding ConditionTypes}" 
                                              SelectedItem="{Binding SelectedPostConditionType, Mode=TwoWay}"/>
                                </DockPanel>

                                <ContentControl Content="{Binding SelectedSequence.PostCondition}"
                                                BorderBrush="LightGray" BorderThickness="1" Padding="5" Margin="0,0,0,10">
                                    <ContentControl.Resources>
                                        <!-- PROXY BINDING for Post-Condition (Reuse structure but bind to Post Proxy) -->
                                        <DataTemplate DataType="{x:Type models:ImageMatchCondition}">
                                            <StackPanel Margin="5">
                                                <DockPanel LastChildFill="True" Margin="0,0,0,5">
                                                    <TextBlock Text="이미지:" VerticalAlignment="Center" Width="60"/>
                                                    <StackPanel Orientation="Horizontal" DockPanel.Dock="Right">
                                                                                                        <Button Content="파일 선택" Width="70" Margin="5,0,0,0" Command="{Binding DataContext.SelectImageCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding .}" ToolTip="Select existing image file"/>
                                                                                                        <Button Content="화면 캡처" Width="70" Margin="5,0,0,0" Command="{Binding DataContext.CaptureImageCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding .}" ToolTip="Snip from screen"/>
                                                                                                        <Button Content="Ref 갱신" Width="60" Margin="5,0,0,0" Command="{Binding DataContext.UpdateRefSizeCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding .}" ToolTip="현재 창 크기로 기준 해상도(Ref Size)를 갱신합니다." Background="#E0F7FA"/>
                                                                                                    </StackPanel>                                                    <TextBox Text="{Binding ImagePath, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Center" IsReadOnly="True"/>
                                                </DockPanel>
                                                <Border BorderBrush="#DDD" BorderThickness="1" Margin="0,0,0,5" Height="100" Background="#F8F8F8">
                                                    <Image Source="{Binding ImagePath, Converter={StaticResource UriToBitmapConverter}}" Stretch="Uniform"/>
                                                </Border>
                                                <DockPanel LastChildFill="True" Margin="0,0,0,5">
                                                    <TextBlock Text="일치율:" VerticalAlignment="Center" Width="100"/>
                                                    <TextBlock Text="{Binding Threshold, StringFormat=P0}" DockPanel.Dock="Right" Width="40" TextAlignment="Right"/>
                                                    <Slider Minimum="0.1" Maximum="1.0" Value="{Binding Threshold}" TickFrequency="0.1" IsSnapToTickEnabled="True"/>
                                                </DockPanel>

                                                <UniformGrid Columns="2" Margin="0,0,0,5">
                                                    <DockPanel Margin="0,0,5,0">
                                                        <TextBlock Text="재시도 횟수:" VerticalAlignment="Center" Width="70"/>
                                                        <TextBox Text="{Binding MaxSearchCount, UpdateSourceTrigger=PropertyChanged}" ToolTip="이미지를 찾을 때까지 반복 시도할 최대 횟수입니다."/>
                                                    </DockPanel>
                                                    <DockPanel>
                                                        <TextBlock Text="재시도 간격:" VerticalAlignment="Center" Width="70"/>
                                                        <TextBox Text="{Binding SearchIntervalMs, UpdateSourceTrigger=PropertyChanged}" ToolTip="재시도 사이의 대기 시간(ms)입니다."/>
                                                        <TextBlock Text="ms" VerticalAlignment="Center" Margin="5,0,0,0"/>
                                                    </DockPanel>
                                                </UniformGrid>

                                                <!-- Test Section -->
                                                <Border Background="#E3F2FD" CornerRadius="4" Padding="8" Margin="0,0,0,10">
                                                    <StackPanel>
                                                        <Grid Margin="0,0,0,5">
                                                            <Grid.ColumnDefinitions>
                                                                <ColumnDefinition Width="*"/>
                                                                <ColumnDefinition Width="Auto"/>
                                                            </Grid.ColumnDefinitions>
                                                            <StackPanel>
                                                                <StackPanel Orientation="Horizontal" Margin="0,0,0,2">
                                                                    <TextBlock Text="Match Score: " FontWeight="Bold"/>
                                                                    <TextBlock Text="{Binding TestScore, StringFormat=P1}" Foreground="#1565C0" FontWeight="Bold"/>
                                                                </StackPanel>
                                                                <StackPanel Orientation="Horizontal">
                                                                    <TextBlock Text="Result: " FontWeight="Bold"/>
                                                                    <TextBlock Text="{Binding TestResult}" Foreground="#0D47A1"/>
                                                                </StackPanel>
                                                            </StackPanel>
                                                            <Button Grid.Column="1" Content="🔍 Test Match" 
                                                                    Padding="10,5" Background="#2196F3" Foreground="White" FontWeight="Bold"
                                                                    Command="{Binding DataContext.TestImageConditionCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                    CommandParameter="{Binding .}"/>
                                                        </Grid>
                                                        
                                                        <!-- Test Result Image Preview -->
                                                        <Border BorderBrush="#90CAF9" BorderThickness="1" Background="White" Margin="0,5,0,0">
                                                            <Border.Style>
                                                                <Style TargetType="Border">
                                                                    <Setter Property="Visibility" Value="Visible"/>
                                                                    <Style.Triggers>
                                                                        <DataTrigger Binding="{Binding DataContext.TestResultImage, RelativeSource={RelativeSource AncestorType=UserControl}}" Value="{x:Null}">
                                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                                        </DataTrigger>
                                                                    </Style.Triggers>
                                                                </Style>
                                                            </Border.Style>
                                                            <Image Source="{Binding DataContext.TestResultImage, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                                                   MaxHeight="400" Stretch="Uniform" ToolTip="Matching Result (Red: Found, Blue: ROI)"/>
                                                        </Border>
                                                    </StackPanel>
                                                </Border>

                                                <!-- ROI Settings (Restored) -->
                                                <GroupBox Header="검색 영역 (ROI)" Margin="0,5,0,10">
                                                    <StackPanel>
                                                        <CheckBox Content="영역 지정 사용 (ROI)" IsChecked="{Binding UseRegion}" Margin="0,0,0,5"/>
                                                        <Grid IsEnabled="{Binding UseRegion}">
                                                            <Grid.ColumnDefinitions>
                                                                <ColumnDefinition Width="*"/>
                                                                <ColumnDefinition Width="Auto"/>
                                                            </Grid.ColumnDefinitions>
                                                            
                                                            <StackPanel Orientation="Vertical">
                                                                <UniformGrid Columns="2" Margin="0,0,0,5">
                                                                    <DockPanel Margin="0,0,5,0">
                                                                        <TextBlock Text="X:" VerticalAlignment="Center" Width="20" FontWeight="Bold"/>
                                                                        <TextBox Text="{Binding RegionX, UpdateSourceTrigger=PropertyChanged}"/>
                                                                    </DockPanel>
                                                                    <DockPanel>
                                                                        <TextBlock Text="Y:" VerticalAlignment="Center" Width="20" FontWeight="Bold"/>
                                                                        <TextBox Text="{Binding RegionY, UpdateSourceTrigger=PropertyChanged}"/>
                                                                    </DockPanel>
                                                                </UniformGrid>
                                                                <UniformGrid Columns="2">
                                                                    <DockPanel Margin="0,0,5,0">
                                                                        <TextBlock Text="W:" VerticalAlignment="Center" Width="20" FontWeight="Bold"/>
                                                                        <TextBox Text="{Binding RegionW, UpdateSourceTrigger=PropertyChanged}"/>
                                                                    </DockPanel>
                                                                    <DockPanel>
                                                                        <TextBlock Text="H:" VerticalAlignment="Center" Width="20" FontWeight="Bold"/>
                                                                        <TextBox Text="{Binding RegionH, UpdateSourceTrigger=PropertyChanged}"/>
                                                                    </DockPanel>
                                                                </UniformGrid>
                                                            </StackPanel>
                                                            
                                                            <Button Grid.Column="1" Content="영역 선택" 
                                                                    Margin="10,0,0,0" Padding="10,0"
                                                                    Command="{Binding DataContext.PickRegionCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                    CommandParameter="{Binding .}"/>
                                                        </Grid>
                                                    </StackPanel>
                                                </GroupBox>

                                                <!-- PROXY BINDING HERE -->
                                                <DockPanel>
                                                    <TextBlock Text="실패 시 이동:" VerticalAlignment="Center" Width="80" Foreground="#C62828" FontWeight="Bold"/>
                                                    <components:JumpTargetSelector ItemsSource="{Binding DataContext.JumpTargets, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                                  SelectedId="{Binding DataContext.SelectedStepPostConditionFailJumpId, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=TwoWay}"/>
                                                </DockPanel>
                                            </StackPanel>
                                        </DataTemplate>

                                        <!-- Proxy Binding Template for SwitchCaseCondition (Post) -->
                                        <DataTemplate DataType="{x:Type models:SwitchCaseCondition}">
                                            <StackPanel Margin="5">
                                                <DockPanel LastChildFill="True" Margin="0,0,0,5">
                                                    <TextBlock Text="변수명:" VerticalAlignment="Center" Width="70"/>
                                                    <ComboBox IsEditable="True" 
                                                              ItemsSource="{Binding DataContext.AvailableIntVariables, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                              Text="{Binding TargetVariableName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                                                </DockPanel>

                                                <TextBlock Text="케이스 목록 (Cases):" FontWeight="Bold" Margin="0,5,0,5"/>
                                                
                                                <ItemsControl ItemsSource="{Binding DataContext.StepPostSwitchCases, RelativeSource={RelativeSource AncestorType=UserControl}}">
                                                    <ItemsControl.ItemTemplate>
                                                        <DataTemplate>
                                                            <Grid Margin="0,0,0,5">
                                                                <Grid.ColumnDefinitions>
                                                                    <ColumnDefinition Width="Auto"/><ColumnDefinition Width="60"/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/>
                                                                </Grid.ColumnDefinitions>
                                                                <TextBlock Text="값(Int):" VerticalAlignment="Center" Margin="0,0,5,0"/>
                                                                <TextBox Grid.Column="1" Text="{Binding CaseValue, UpdateSourceTrigger=PropertyChanged}"/>
                                                                <TextBlock Grid.Column="2" Text="→ 이동:" VerticalAlignment="Center" Margin="10,0,5,0"/>
                                                                <!-- Direct Binding for Case Jump -->
                                                                <components:JumpTargetSelector Grid.Column="3" 
                                                                                              ItemsSource="{Binding DataContext.JumpTargets, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                                              SelectedId="{Binding JumpId, Mode=TwoWay}"/>
                                                                <Button Grid.Column="4" Content="-" Width="25" Margin="5,0,0,0" Background="#FFEBEE" Foreground="Red"
                                                                        Command="{Binding DataContext.RemoveSwitchCaseCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                        CommandParameter="{Binding .}"/>
                                                            </Grid>
                                                        </DataTemplate>
                                                    </ItemsControl.ItemTemplate>
                                                </ItemsControl>

                                                <Button Content="+ 케이스 추가" 
                                                        Command="{Binding DataContext.AddSwitchCaseCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                        CommandParameter="{Binding .}"
                                                        Margin="0,5,0,10" Background="#E8F5E9"/>

                                                <!-- Proxy Binding for Fail Jump -->
                                                <DockPanel>
                                                    <TextBlock Text="실패 시 이동:" VerticalAlignment="Center" Width="80" Foreground="#C62828" FontWeight="Bold"/>
                                                    <components:JumpTargetSelector ItemsSource="{Binding DataContext.JumpTargets, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                                  SelectedId="{Binding DataContext.SelectedStepPostConditionFailJumpId, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=TwoWay}"/>
                                                </DockPanel>
                                            </StackPanel>
                                        </DataTemplate>

                                                                                                                        <!-- Proxy Binding for ProcessRunningCondition (Post) -->

                                                                                                                        <DataTemplate DataType="{x:Type models:ProcessRunningCondition}">

                                                                                                                            <StackPanel Margin="5">

                                                                                                                                <!-- Input Source Selection -->

                                                                                                                                <TextBlock Text="입력 소스 (Input Source):" Margin="0,0,0,5" Foreground="Gray"/>

                                                                                                                                <StackPanel Orientation="Horizontal" Margin="0,0,0,10">

                                                                                                                                    <RadioButton Content="직접 입력 (Direct)" GroupName="ProcRunPostSrc" Margin="0,0,15,0"

                                                                                                                                                 IsChecked="{Binding SourceType, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter=Direct}"/>

                                                                                                                                    <RadioButton Content="변수값 (Variable)" GroupName="ProcRunPostSrc"

                                                                                                                                                 IsChecked="{Binding SourceType, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter=Variable}"

                                                                                                                                                 ToolTip="변수에 저장된 문자열을 대상 이름으로 사용합니다."/>

                                                                                                                                </StackPanel>

                                                                                

                                                                                                                                <DockPanel LastChildFill="True" Margin="0,0,0,10">

                                                                                                                                    <TextBlock Text="검색 기준:" VerticalAlignment="Center" Width="70"/>

                                                                                                                                    <ComboBox ItemsSource="{Binding DataContext.SearchMethods, RelativeSource={RelativeSource AncestorType=UserControl}}"

                                                                                                                                              SelectedItem="{Binding SearchMethod}"/>

                                                                                                                                </DockPanel>

                                                                                

                                                                                                                                <!-- Direct Mode Input -->

                                                                                                                                <DockPanel LastChildFill="True" Margin="0,0,0,10">

                                                                                                                                    <DockPanel.Style>

                                                                                                                                        <Style TargetType="DockPanel">

                                                                                                                                            <Setter Property="Visibility" Value="Collapsed"/>

                                                                                                                                            <Style.Triggers>

                                                                                                                                                <DataTrigger Binding="{Binding SourceType}" Value="Direct">

                                                                                                                                                    <Setter Property="Visibility" Value="Visible"/>

                                                                                                                                                </DataTrigger>

                                                                                                                                            </Style.Triggers>

                                                                                                                                        </Style>

                                                                                                                                    </DockPanel.Style>

                                                                                                                                    <TextBlock Text="대상 이름:" VerticalAlignment="Center" Width="70"/>

                                                                                                                                                        <Button DockPanel.Dock="Right" Content="⟳" Width="25" Margin="5,0,0,0"

                                                                                                                                                                Command="{Binding DataContext.RefreshContextTargetCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"

                                                                                                                                                                CommandParameter="{Binding .}"

                                                                                                                                                                ToolTip="Refresh Process List"/>                                                    <components:VariableSelector ItemsSource="{Binding DataContext.ProcessList, RelativeSource={RelativeSource AncestorType=UserControl}}"

                                                                                                                                                                                        Text="{Binding ProcessName, Mode=TwoWay}"

                                                                                                                                                                                        ToolTip="Process Name or Window Title Part"/>

                                                                                                                                </DockPanel>

                                                                                

                                                                                                                                <!-- Variable Mode Input -->

                                                                                                                                <DockPanel LastChildFill="True" Margin="0,0,0,10">

                                                                                                                                    <DockPanel.Style>

                                                                                                                                        <Style TargetType="DockPanel">

                                                                                                                                            <Setter Property="Visibility" Value="Collapsed"/>

                                                                                                                                            <Style.Triggers>

                                                                                                                                                <DataTrigger Binding="{Binding SourceType}" Value="Variable">

                                                                                                                                                    <Setter Property="Visibility" Value="Visible"/>

                                                                                                                                                </DataTrigger>

                                                                                                                                            </Style.Triggers>

                                                                                                                                        </Style>

                                                                                                                                    </DockPanel.Style>

                                                                                                                                    <TextBlock Text="변수명:" VerticalAlignment="Center" Width="70"/>

                                                                                                                                    <components:VariableSelector ItemsSource="{Binding DataContext.AvailableIntVariables, RelativeSource={RelativeSource AncestorType=UserControl}}"

                                                                                                                                                                Text="{Binding ProcessNameVariable, Mode=TwoWay}"

                                                                                                                                                                ToolTip="대상 이름을 담고 있는 변수를 선택하세요."/>

                                                                                                                                </DockPanel>                                                <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                                                    <TextBlock Text="조건 확인:" VerticalAlignment="Center" Width="70" Margin="0,0,5,0"/>
                                                    <RadioButton Content="실행 중 (Running)" GroupName="ProcRunPost" 
                                                                 IsChecked="{Binding IsCheckRunning, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter=True}" 
                                                                 Margin="0,0,15,0"/>
                                                    <RadioButton Content="종료됨 (Not Running)" GroupName="ProcRunPost"
                                                                 IsChecked="{Binding IsCheckRunning, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter=False}"/>
                                                </StackPanel>

                                                <UniformGrid Columns="2" Margin="0,0,0,10">
                                                    <DockPanel Margin="0,0,5,0">
                                                        <TextBlock Text="재시도 횟수:" VerticalAlignment="Center" Width="70"/>
                                                        <TextBox Text="{Binding MaxSearchCount, UpdateSourceTrigger=PropertyChanged}" ToolTip="조건을 만족할 때까지 반복 시도할 최대 횟수입니다."/>
                                                    </DockPanel>
                                                    <DockPanel>
                                                        <TextBlock Text="재시도 간격:" VerticalAlignment="Center" Width="70"/>
                                                        <TextBox Text="{Binding SearchIntervalMs, UpdateSourceTrigger=PropertyChanged}" ToolTip="재시도 사이의 대기 시간(ms)입니다."/>
                                                        <TextBlock Text="ms" VerticalAlignment="Center" Margin="5,0,0,0"/>
                                                    </DockPanel>
                                                </UniformGrid>

                                                <DockPanel>
                                                    <TextBlock Text="실패 시 이동:" VerticalAlignment="Center" Width="80" Foreground="#C62828" FontWeight="Bold"/>
                                                    <components:JumpTargetSelector ItemsSource="{Binding DataContext.JumpTargets, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                                  SelectedId="{Binding DataContext.SelectedStepPostConditionFailJumpId, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=TwoWay}"/>
                                                </DockPanel>
                                            </StackPanel>
                                        </DataTemplate>
                                    </ContentControl.Resources>
                                </ContentControl>

                                <Separator Margin="0,5,0,10"/>

                                <Grid DataContext="{Binding SelectedSequence}">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <StackPanel Grid.Column="0" Margin="0,0,10,0">
                                        <TextBlock Text="[성공 시] 흐름 제어 (다음 단계)" Foreground="#2E7D32" FontWeight="Bold" Margin="0,0,0,5"/>
                                        <!-- PROXY BINDING for Success Jump -->
                                        <components:JumpTargetSelector ItemsSource="{Binding DataContext.JumpTargets, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                      SelectedId="{Binding DataContext.SelectedStepSuccessJumpId, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=TwoWay}"/>
                                    </StackPanel>
                                </Grid>
                            </StackPanel>
                        </GroupBox>
                    </StackPanel>
                </ScrollViewer>
            </Grid>
            <!-- Placeholder -->
            <TextBlock Text="편집할 그룹이나 스텝을 선택하세요" 
                       HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="Gray" FontSize="16">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <MultiDataTrigger>
                                <MultiDataTrigger.Conditions>
                                    <Condition Binding="{Binding SelectedGroup}" Value="{x:Null}"/>
                                    <Condition Binding="{Binding SelectedSequence}" Value="{x:Null}"/>
                                </MultiDataTrigger.Conditions>
                                <Setter Property="Visibility" Value="Visible"/>
                            </MultiDataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>
        </Grid>

        <!-- Variable Manager Overlay -->
        <Grid Grid.ColumnSpan="2" Background="#80000000" Visibility="{Binding IsVariableManagerOpen, Converter={StaticResource BooleanToVisibilityConverter}}">
             <!-- ... (Variable Manager Content Unchanged) ... -->
            <Border Background="White" Width="500" Height="400" CornerRadius="10" Padding="20">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Text="변수 관리자 (Variable Manager)" FontSize="18" FontWeight="Bold" Margin="0,0,0,20"/>

                    <DataGrid Grid.Row="1" ItemsSource="{Binding DefinedVariables}" AutoGenerateColumns="False" CanUserAddRows="False" Margin="0,0,0,10">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="변수명 (Name)" Binding="{Binding Name}" Width="120"/>
                            <DataGridTextColumn Header="기본값 (Default)" Binding="{Binding DefaultValue}" Width="100"/>
                            <DataGridTextColumn Header="설명 (Description)" Binding="{Binding Description}" Width="*"/>
                            <DataGridTemplateColumn Width="50">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Content="❌" Command="{Binding DataContext.RemoveVariableDefinitionCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding .}" Background="Transparent" BorderThickness="0" Foreground="Red"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>

                    <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="+ 변수 추가" Command="{Binding AddVariableDefinitionCommand}" Padding="10,5" Margin="0,0,10,0" Background="#E0F7FA"/>
                        <Button Content="닫기 (저장)" Command="{Binding CloseVariableManagerCommand}" Padding="20,5" Background="#2196F3" Foreground="White"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>